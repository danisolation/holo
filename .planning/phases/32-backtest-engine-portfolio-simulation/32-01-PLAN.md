---
phase: 32-backtest-engine-portfolio-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/backtest.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/014_backtest_tables.py
  - backend/app/schemas/backtest.py
  - backend/app/api/backtest.py
  - backend/app/api/router.py
autonomous: true
requirements: [BT-01, BT-05, SIM-01, SIM-03]

must_haves:
  truths:
    - "BacktestRun, BacktestTrade, BacktestEquity, BacktestAnalysis models exist with all required columns"
    - "Alembic migration 014 creates 4 backtest tables with correct types, constraints, and indexes"
    - "POST /api/backtest/runs validates date range (1-6 months), capital, slippage and creates a run"
    - "GET /api/backtest/runs/{id} returns run status and progress"
    - "POST /api/backtest/runs/{id}/cancel sets is_cancelled flag"
    - "Singleton enforcement: cannot start a new backtest while one is RUNNING"
  artifacts:
    - path: "backend/app/models/backtest.py"
      provides: "BacktestRun, BacktestTrade, BacktestEquity, BacktestAnalysis, BacktestStatus models"
      contains: "class BacktestRun"
    - path: "backend/alembic/versions/014_backtest_tables.py"
      provides: "Database migration for 4 backtest tables"
      contains: "backtest_runs"
    - path: "backend/app/schemas/backtest.py"
      provides: "Pydantic request/response schemas for backtest API"
      contains: "class BacktestStartRequest"
    - path: "backend/app/api/backtest.py"
      provides: "REST endpoints: start, status, cancel, trades, equity"
      contains: "router = APIRouter"
  key_links:
    - from: "backend/app/api/backtest.py"
      to: "backend/app/models/backtest.py"
      via: "SQLAlchemy model imports"
      pattern: "from app.models.backtest import"
    - from: "backend/app/api/router.py"
      to: "backend/app/api/backtest.py"
      via: "router include"
      pattern: "include_router.*backtest"
---

<objective>
Create the database foundation and API layer for the backtest engine.

Purpose: Establish all persistence (4 tables via Alembic) and REST endpoints needed before the engine core can be built. This plan delivers the schema contracts and API shell that Plan 02 and Plan 03 implement against.

Output: 4 SQLAlchemy models, 1 Alembic migration, Pydantic schemas, 6 API endpoints wired into the main router.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-backtest-engine-portfolio-simulation/32-CONTEXT.md
@.planning/phases/32-backtest-engine-portfolio-simulation/32-RESEARCH.md

@backend/app/models/paper_trade.py
@backend/app/models/__init__.py
@backend/app/models/ai_analysis.py
@backend/alembic/versions/013_paper_trade_tables.py
@backend/app/api/paper_trading.py
@backend/app/api/router.py
@backend/app/database.py
@backend/app/schemas/paper_trading.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/models/paper_trade.py:
```python
class TradeStatus(str, enum.Enum):
    PENDING = "pending"
    ACTIVE = "active"
    PARTIAL_TP = "partial_tp"
    CLOSED_TP2 = "closed_tp2"
    CLOSED_SL = "closed_sl"
    CLOSED_TIMEOUT = "closed_timeout"
    CLOSED_MANUAL = "closed_manual"

class TradeDirection(str, enum.Enum):
    LONG = "long"
    BEARISH = "bearish"
```

From backend/app/models/__init__.py:
```python
from sqlalchemy.orm import DeclarativeBase
class Base(DeclarativeBase):
    pass
# All models imported for Alembic detection
```

From backend/app/database.py:
```python
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
async def get_db():
    async with async_session() as session:
        yield session
```

From backend/app/api/router.py:
```python
api_router = APIRouter()
api_router.include_router(system_router)
# ... 7 existing routers
api_router.include_router(paper_trading_router)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backtest database models + Alembic migration</name>
  <files>backend/app/models/backtest.py, backend/app/models/__init__.py, backend/alembic/versions/014_backtest_tables.py, backend/tests/test_backtest_models.py</files>
  <behavior>
    - Test: BacktestStatus enum has 4 values: running, completed, cancelled, failed
    - Test: BacktestRun model has all required columns (id, start_date, end_date, initial_capital, slippage_pct, status, last_completed_date, total_sessions, completed_sessions, is_cancelled, created_at, updated_at)
    - Test: BacktestTrade model mirrors PaperTrade with added run_id and backtest_analysis_id FKs
    - Test: BacktestTrade.effective_stop_loss property returns adjusted_stop_loss when set, else stop_loss
    - Test: BacktestEquity model has run_id, date, cash, positions_value, total_equity, daily_return_pct, cumulative_return_pct
    - Test: BacktestAnalysis model has run_id, ticker_id, analysis_type (String not Enum), analysis_date, signal, score, reasoning, raw_response, with unique constraint on (run_id, ticker_id, analysis_type, analysis_date)
    - Test: BacktestRun.initial_capital defaults to 100000000
    - Test: BacktestRun.slippage_pct defaults to 0.5
  </behavior>
  <action>
1. Create `backend/app/models/backtest.py` with 4 SQLAlchemy models exactly matching the RESEARCH.md code examples:
   - `BacktestStatus(str, enum.Enum)` with RUNNING, COMPLETED, CANCELLED, FAILED
   - `BacktestRun(Base)` — table "backtest_runs" with: id (BigInteger PK), start_date (Date), end_date (Date), initial_capital (Numeric(16,2) default 100000000), slippage_pct (Numeric(5,4) default 0.5), status (SAEnum BacktestStatus default "running"), last_completed_date (Date nullable), total_sessions (Integer default 0), completed_sessions (Integer default 0), is_cancelled (Boolean default false), created_at (TIMESTAMP), updated_at (TIMESTAMP with onupdate)
   - `BacktestTrade(Base)` — table "backtest_trades" mirroring PaperTrade exactly but with: run_id (BigInteger FK→backtest_runs.id), backtest_analysis_id (BigInteger FK→backtest_analyses.id nullable). Reuse `TradeStatus` and `TradeDirection` enums from paper_trade.py (import, don't recreate). Include `effective_stop_loss` property.
   - `BacktestAnalysis(Base)` — table "backtest_analyses" with: id (BigInteger PK), run_id (BigInteger FK), ticker_id (Integer FK→tickers.id), analysis_type (String(20) — NOT Enum per RESEARCH.md A3), analysis_date (Date), signal (String(20)), score (Integer), reasoning (Text), model_version (String(50)), raw_response (JSONB nullable), created_at. UniqueConstraint on (run_id, ticker_id, analysis_type, analysis_date).
   - `BacktestEquity(Base)` — table "backtest_equity" with: id (BigInteger PK), run_id (BigInteger FK), date (Date), cash (Numeric(16,2)), positions_value (Numeric(16,2)), total_equity (Numeric(16,2)), daily_return_pct (Float nullable), cumulative_return_pct (Float nullable). UniqueConstraint on (run_id, date).

2. Update `backend/app/models/__init__.py` — add import line:
   ```python
   from app.models.backtest import BacktestRun, BacktestTrade, BacktestEquity, BacktestAnalysis, BacktestStatus  # noqa: E402
   ```
   Add to `__all__` list: `"BacktestRun", "BacktestTrade", "BacktestEquity", "BacktestAnalysis", "BacktestStatus"`

3. Create `backend/alembic/versions/014_backtest_tables.py` following the exact pattern from 013_paper_trade_tables.py (raw SQL via op.execute):
   - revision="014", down_revision="013"
   - CREATE TYPE backtest_status AS ENUM ('running', 'completed', 'cancelled', 'failed')
   - CREATE TABLE backtest_runs (all columns, PK, defaults, CHECK constraints)
   - CREATE TABLE backtest_analyses (all columns, PK, FK, unique constraint uq_backtest_analyses_run_ticker_type_date)
   - CREATE TABLE backtest_trades (all columns, PK, FKs to backtest_runs and backtest_analyses and tickers, reuse existing trade_status and trade_direction enums — do NOT recreate them)
   - CREATE TABLE backtest_equity (all columns, PK, FK, unique constraint uq_backtest_equity_run_date)
   - CREATE INDEX on: backtest_trades(run_id), backtest_trades(status), backtest_trades(run_id, signal_date), backtest_analyses(run_id, analysis_date), backtest_equity(run_id, date)
   - downgrade: DROP tables in reverse order (equity, trades, analyses, runs), DROP TYPE backtest_status

4. Create `backend/tests/test_backtest_models.py` with unit tests (pure Python, no DB) covering all behaviors listed above. Follow the pattern from test_paper_trade_model.py.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_backtest_models.py -x -v</automated>
  </verify>
  <done>4 model classes defined, migration file exists with upgrade/downgrade, all model tests pass, models/__init__.py exports new models</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic schemas + API endpoints + router wiring</name>
  <files>backend/app/schemas/backtest.py, backend/app/api/backtest.py, backend/app/api/router.py, backend/tests/test_backtest_api.py</files>
  <behavior>
    - Test: BacktestStartRequest validates start_date < end_date
    - Test: BacktestStartRequest rejects date ranges > 6 months (180 days)
    - Test: BacktestStartRequest rejects date ranges < 1 month (20 days)
    - Test: BacktestStartRequest defaults: initial_capital=100_000_000, slippage_pct=0.5
    - Test: BacktestStartRequest validates slippage_pct range 0.0 to 5.0
    - Test: BacktestStartRequest validates initial_capital > 0
    - Test: POST /api/backtest/runs returns 409 when backtest already running (singleton)
    - Test: POST /api/backtest/runs/{id}/cancel sets is_cancelled=True
    - Test: GET /api/backtest/runs/{id} returns run with progress fields
  </behavior>
  <action>
1. Create `backend/app/schemas/backtest.py` with Pydantic v2 models:
   - `BacktestStartRequest(BaseModel)`: start_date (date), end_date (date), initial_capital (float, default=100_000_000, gt=0), slippage_pct (float, default=0.5, ge=0.0, le=5.0). Add `@model_validator(mode="after")` that checks: (a) start_date < end_date, (b) date range is 20-180 days (approx 1-6 months).
   - `BacktestRunResponse(BaseModel)`: id (int), start_date (str), end_date (str), initial_capital (float), slippage_pct (float), status (str), last_completed_date (str|None), total_sessions (int), completed_sessions (int), is_cancelled (bool), created_at (str), updated_at (str). Add computed property `progress_pct` = completed_sessions/total_sessions*100 if total_sessions > 0 else 0.
   - `BacktestTradeResponse(BaseModel)`: mirrors PaperTradeResponse with added run_id (int) and backtest_analysis_id (int|None) fields.
   - `BacktestEquityResponse(BaseModel)`: run_id (int), date (str), cash (float), positions_value (float), total_equity (float), daily_return_pct (float|None), cumulative_return_pct (float|None).
   - `BacktestTradeListResponse(BaseModel)`: trades (list[BacktestTradeResponse]), total (int).
   - `BacktestEquityListResponse(BaseModel)`: equity (list[BacktestEquityResponse]), total (int).

2. Create `backend/app/api/backtest.py` — FastAPI router following paper_trading.py pattern:
   - `router = APIRouter(prefix="/backtest", tags=["backtest"])`
   - `POST /runs` (status_code=201) — per BT-01, SIM-01, SIM-03:
     * Validate request via BacktestStartRequest
     * Check singleton: query BacktestRun WHERE status='running'. If exists → raise HTTPException(409, "A backtest is already running. Cancel it first or wait for completion.")
     * Query trading dates: `SELECT DISTINCT date FROM daily_prices WHERE date BETWEEN start AND end ORDER BY date` to count total_sessions
     * Create BacktestRun with status=RUNNING, total_sessions=count
     * Commit and return {id, status, total_sessions}
     * NOTE: Do NOT launch background task yet — Plan 02 wires the engine
   - `GET /runs/latest` — returns the most recent BacktestRun (ORDER BY created_at DESC LIMIT 1) or 404
   - `GET /runs/{run_id}` — returns BacktestRunResponse or 404
   - `POST /runs/{run_id}/cancel` — per BT-05: set is_cancelled=True on RUNNING run, else 404/400
   - `GET /runs/{run_id}/trades` — returns BacktestTradeListResponse with ticker symbol JOIN, optional status/direction query params
   - `GET /runs/{run_id}/equity` — returns BacktestEquityListResponse ordered by date ASC

   Use `async_session()` context manager pattern (same as paper_trading.py). Import from `app.database import async_session`.

3. Update `backend/app/api/router.py` — add:
   ```python
   from app.api.backtest import router as backtest_router
   api_router.include_router(backtest_router)
   ```

4. Create `backend/tests/test_backtest_api.py` with unit tests:
   - Test BacktestStartRequest validation (date range limits, defaults, slippage bounds)
   - Test schema serialization for responses
   - Test endpoint logic with mocked DB sessions (follow test_paper_trade_api.py pattern)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_backtest_api.py tests/test_backtest_models.py -x -v</automated>
  </verify>
  <done>6 API endpoints functional, Pydantic schemas validate all inputs, singleton enforcement works, router wired, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User submits backtest config (dates, capital, slippage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | BacktestStartRequest | mitigate | Pydantic validates date range (20-180 days), capital (>0), slippage (0-5%) at route entry |
| T-32-02 | Denial of Service | POST /runs | mitigate | Singleton pattern: only 1 backtest at a time, reject 409 if running |
| T-32-03 | Information Disclosure | backtest_analyses | accept | Single-user app, no PII in backtest data |
</threat_model>

<verification>
1. `cd backend && python -m pytest tests/test_backtest_models.py tests/test_backtest_api.py -x -v` — all tests pass
2. `cd backend && python -c "from app.models.backtest import BacktestRun, BacktestTrade, BacktestEquity, BacktestAnalysis, BacktestStatus; print('Models OK')"` — imports succeed
3. `cd backend && python -c "from app.schemas.backtest import BacktestStartRequest; r = BacktestStartRequest(start_date='2025-01-01', end_date='2025-06-01'); print(f'Valid: {r.start_date} to {r.end_date}')"` — schema validates
4. Migration file exists: `backend/alembic/versions/014_backtest_tables.py`
</verification>

<success_criteria>
- 4 SQLAlchemy models (BacktestRun, BacktestTrade, BacktestEquity, BacktestAnalysis) with all columns matching CONTEXT.md decisions
- Alembic migration 014 creates all 4 tables with proper types, constraints, indexes, and clean downgrade
- Pydantic schemas validate: date range 1-6 months, capital > 0, slippage 0-5%
- 6 API endpoints (POST /runs, GET /runs/latest, GET /runs/{id}, POST /runs/{id}/cancel, GET /runs/{id}/trades, GET /runs/{id}/equity)
- Singleton enforcement: 409 if backtest already running
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-backtest-engine-portfolio-simulation/32-01-SUMMARY.md`
</output>
