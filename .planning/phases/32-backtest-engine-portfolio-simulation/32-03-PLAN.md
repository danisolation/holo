---
phase: 32-backtest-engine-portfolio-simulation
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - backend/tests/test_backtest_engine.py
  - backend/tests/test_backtest_analysis_service.py
  - backend/app/services/backtest_engine.py
  - backend/app/services/backtest_analysis_service.py
autonomous: false
requirements: [BT-02, BT-03, BT-04, BT-05, BT-06, SIM-02, SIM-03, SIM-04]

must_haves:
  truths:
    - "Full backtest test suite passes covering all 10 requirements"
    - "BacktestAnalysisService date-awareness verified (no queries without date bounds)"
    - "Engine integration test verifies complete 3-day cycle: signal → pending → active → evaluate → equity"
    - "Checkpoint/resume test verifies engine skips processed dates and reloads state"
    - "Existing test suite (560 tests) unaffected — no regressions"
  artifacts:
    - path: "backend/tests/test_backtest_analysis_service.py"
      provides: "Unit tests for date-aware analysis service"
      min_lines: 80
    - path: "backend/tests/test_backtest_engine.py"
      provides: "Comprehensive engine tests including integration scenarios"
      min_lines: 200
  key_links:
    - from: "backend/tests/test_backtest_engine.py"
      to: "backend/app/services/backtest_engine.py"
      via: "test imports"
      pattern: "from app.services.backtest_engine import"
    - from: "backend/tests/test_backtest_analysis_service.py"
      to: "backend/app/services/backtest_analysis_service.py"
      via: "test imports"
      pattern: "from app.services.backtest_analysis_service import"
---

<objective>
Comprehensive test coverage for the backtest engine and verification that all 10 phase requirements are satisfied with no regressions to the existing 560-test suite.

Purpose: The backtest engine is a complex 53-hour compute workload. Thorough unit tests for date-aware analysis, position lifecycle, slippage, equity tracking, and checkpoint/resume are essential before any real execution. This plan also fixes any edge cases found during test creation.

Output: Complete test files covering all requirements + any bug fixes discovered during testing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-backtest-engine-portfolio-simulation/32-CONTEXT.md
@.planning/phases/32-backtest-engine-portfolio-simulation/32-RESEARCH.md
@.planning/phases/32-backtest-engine-portfolio-simulation/32-01-SUMMARY.md
@.planning/phases/32-backtest-engine-portfolio-simulation/32-02-SUMMARY.md

@backend/app/services/backtest_engine.py
@backend/app/services/backtest_analysis_service.py
@backend/app/models/backtest.py
@backend/app/schemas/backtest.py
@backend/app/api/backtest.py
@backend/tests/test_backtest_engine.py
@backend/tests/test_backtest_models.py
@backend/tests/test_backtest_api.py
@backend/tests/test_paper_trade_model.py

<interfaces>
<!-- Key contracts from Plan 01 + Plan 02 outputs -->

From backend/app/services/backtest_engine.py (Plan 02):
```python
def apply_slippage(price: Decimal, slippage_pct: Decimal, is_buy: bool) -> Decimal
class BacktestEngine:
    async def run(self, run_id: int) -> None
```

From backend/app/services/backtest_analysis_service.py (Plan 02):
```python
class BacktestAnalysisService(AIAnalysisService):
    def __init__(self, session: AsyncSession, run_id: int, as_of_date: date, api_key=None)
    async def _get_technical_context(self, ticker_id, symbol) -> dict | None  # date-aware
    async def _get_combined_context(self, ticker_id, symbol) -> dict | None  # reads backtest_analyses
    async def _get_trading_signal_context(self, ticker_id, symbol) -> dict | None  # date-aware
    async def _store_analysis(self, ticker_id, analysis_type, analysis_date, signal, score, reasoning, raw_response)  # writes backtest_analyses
```

From backend/app/services/paper_trade_service.py:
```python
def calculate_position_size(capital, allocation_pct, entry_price) -> int
def calculate_pnl(direction, entry_price, quantity, partial_exit_price, closed_quantity, exit_price) -> tuple
def apply_partial_tp(trade, tp1_price) -> None
def evaluate_long_position(status, effective_sl, tp1, tp2, bar_open, bar_high, bar_low) -> tuple
def evaluate_bearish_position(status, effective_sl, tp1, tp2, bar_open, bar_high, bar_low) -> tuple
TIMEOUT_TRADING_DAYS = {"swing": 15, "position": 60}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestAnalysisService tests + engine integration tests</name>
  <files>backend/tests/test_backtest_analysis_service.py, backend/tests/test_backtest_engine.py, backend/app/services/backtest_analysis_service.py, backend/app/services/backtest_engine.py</files>
  <behavior>
    - Test: BacktestAnalysisService.__init__ stores run_id and as_of_date
    - Test: _get_technical_context adds date filter (verify query includes as_of_date bound)
    - Test: _get_combined_context reads from BacktestAnalysis model (not AIAnalysis)
    - Test: _get_combined_context filters by run_id
    - Test: _get_trading_signal_context uses as_of_date for 52-week window (not date.today())
    - Test: _store_analysis writes to backtest_analyses table with run_id
    - Test: Full 3-day engine cycle — day 1 generates signal → day 2 activates trade + evaluates → day 3 evaluates position
    - Test: Engine with no trading dates (empty period) completes immediately with status=COMPLETED
    - Test: Engine marks status=FAILED on unhandled exception
    - Test: Capital tracking across multiple trades (open 3 trades → close 1 → verify cash)
    - Test: Equity curve monotonic check — equity after profitable close > equity before
    - Test: Duplicate signal protection — same ticker same day doesn't create duplicate trades
    - Test: BEARISH position evaluation uses inverted logic correctly
  </behavior>
  <action>
1. Create `backend/tests/test_backtest_analysis_service.py` — unit tests for the date-aware analysis service:

**Test class: TestBacktestAnalysisServiceInit**
- Test constructor stores run_id and as_of_date attributes
- Test inherits from AIAnalysisService (isinstance check)

**Test class: TestDateAwareContextMethods**
Use unittest.mock to patch the SQLAlchemy session and verify queries include date bounds.

- `test_technical_context_date_filter`: Mock the session.execute, call _get_technical_context, verify that the SQL query/select includes a filter for `date <= as_of_date`. Use `MagicMock` for session, assert the `where` clause includes the date bound by inspecting the compiled query or by checking call args.
- `test_combined_context_reads_backtest_analyses`: Mock session.execute, call _get_combined_context. Verify the query targets `backtest_analyses` table (BacktestAnalysis model), NOT `ai_analyses`. Check that `run_id` filter is present.
- `test_combined_context_graceful_degradation`: When no sentiment analysis exists, combined context should have `sent_signal="neutral"` and `sent_score=5`.
- `test_trading_signal_context_52week_relative`: Mock session, verify the 52-week high/low query uses `as_of_date - timedelta(365)` to `as_of_date`, NOT `date.today()`.
- `test_store_analysis_targets_backtest_table`: Mock session.execute, call _store_analysis, verify the raw SQL targets `backtest_analyses` and includes `run_id`.

**Test class: TestStoreAnalysisIsolation**
- `test_never_writes_ai_analyses`: Inspect the _store_analysis method source code (or mock and assert) — verify no reference to "ai_analyses" table name in the INSERT statement.

2. Extend `backend/tests/test_backtest_engine.py` with integration-level tests (using mocks for DB and Gemini):

**Test class: TestEngineLifecycle**
- `test_engine_completes_successfully`: Mock the full pipeline (DB queries, analysis service, price data). Verify run.status set to COMPLETED after loop.
- `test_engine_handles_cancel`: Set is_cancelled=True on first cancel check. Verify run.status = CANCELLED.
- `test_engine_handles_exception`: Make analysis service raise an exception. Verify run.status = FAILED.
- `test_engine_empty_date_range`: When trading_dates is empty, engine should complete immediately.

**Test class: TestPositionLifecycle**
- `test_signal_creates_pending_trade`: Mock signal data, verify BacktestTrade created with status=PENDING, correct signal_date, prices from signal.
- `test_pending_activates_at_d_plus_1`: Pending trade with signal_date=D, called on D+1. Verify status→ACTIVE, entry_price=slipped open, entry_date=D+1.
- `test_pending_not_activated_same_day`: signal_date=D, evaluated on D. Should remain PENDING.
- `test_active_long_hits_sl`: Set bar_low below SL. Verify CLOSED_SL with slipped exit price.
- `test_active_long_hits_tp1`: Set bar_high above TP1. Verify PARTIAL_TP with apply_partial_tp called, cash increased.
- `test_partial_tp_hits_tp2`: Already in PARTIAL_TP, bar_high above TP2. Verify CLOSED_TP2, full P&L computed.
- `test_bearish_position_sl`: BEARISH active, bar_high above SL. Verify CLOSED_SL.
- `test_timeout_closes_position`: Active position held beyond TIMEOUT_TRADING_DAYS. Verify CLOSED_TIMEOUT.

**Test class: TestCashAndEquity**
- `test_cash_deducted_on_trade_open`: Cash before − (slipped_entry × quantity) = Cash after.
- `test_cash_returned_on_trade_close`: Cash after close = cash before + (slipped_exit × remaining_qty).
- `test_partial_tp_returns_partial_cash`: On partial TP, cash += slipped_tp1 × closed_quantity (not full quantity).
- `test_equity_snapshot_correct`: Verify total_equity = cash + sum(close_price × remaining_qty) for open positions.
- `test_cumulative_return_pct`: (total_equity - initial_capital) / initial_capital × 100.
- `test_daily_return_pct`: (today_equity - yesterday_equity) / yesterday_equity × 100.

**Test class: TestCheckpointResume**
- `test_checkpoint_updates_run_fields`: After processing a day, last_completed_date and completed_sessions are updated.
- `test_resume_skips_completed_dates`: Set last_completed_date=D2. Verify engine starts from D3.
- `test_resume_reloads_open_positions`: Open position exists in DB for resumed run. Verify it's evaluated on next day.
- `test_resume_restores_cash_from_equity`: Last equity snapshot has cash=X. Verify engine uses X, not initial_capital.

3. **Bug fixes** — if any tests fail due to edge cases in the engine/analysis service code from Plan 02, fix them in-place. Common fixes:
   - Decimal vs float conversion issues in slippage calculation
   - Missing commit() calls after position updates
   - Off-by-one in D+1 activation logic (signal_date < session_date, not <=)
   - Timeout day counting needs to exclude the entry_date itself

Document any fixes in the SUMMARY as "edge cases discovered and fixed during testing."
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_backtest_analysis_service.py tests/test_backtest_engine.py -x -v</automated>
  </verify>
  <done>Analysis service tests verify date-awareness and isolation. Engine tests cover full lifecycle, position management, cash/equity tracking, checkpoint/resume. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete backtest engine — tests + imports + no regressions</name>
  <files>backend/tests/test_backtest_models.py, backend/tests/test_backtest_api.py, backend/tests/test_backtest_engine.py, backend/tests/test_backtest_analysis_service.py</files>
  <action>
Human verifies the complete Phase 32 deliverable — all automated tests, no regressions, correct migration, API registration.

**What was built:**
- 4 database tables (backtest_runs, backtest_trades, backtest_equity, backtest_analyses)
- 6 API endpoints (POST /runs, GET /runs/latest, GET /runs/{id}, POST /runs/{id}/cancel, GET /runs/{id}/trades, GET /runs/{id}/equity)
- Date-aware Gemini analysis service (BacktestAnalysisService)
- Full engine loop with position management, slippage, equity tracking, checkpoint/resume, cancel
- Comprehensive test suite

**Steps to verify:**
1. Run backtest test suite: `cd backend && python -m pytest tests/test_backtest_models.py tests/test_backtest_api.py tests/test_backtest_engine.py tests/test_backtest_analysis_service.py -x -v`
2. Run full existing suite (no regressions): `cd backend && python -m pytest tests/ -x --timeout=120`
3. Check migration: `cat backend/alembic/versions/014_backtest_tables.py` — creates 4 tables
4. Check API routes: `cd backend && python -c "from app.api.router import api_router; routes = [r.path for r in api_router.routes]; print([r for r in routes if 'backtest' in r])"`
5. Check imports: `cd backend && python -c "from app.models.backtest import BacktestRun, BacktestTrade, BacktestEquity, BacktestAnalysis; from app.services.backtest_engine import BacktestEngine; from app.services.backtest_analysis_service import BacktestAnalysisService; print('All OK')"`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_backtest_models.py tests/test_backtest_api.py tests/test_backtest_engine.py tests/test_backtest_analysis_service.py -x -v</automated>
  </verify>
  <done>Human approves: all tests pass, no regressions, migration correct, API routes registered, all imports work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→production code | Tests verify security properties (isolation, date bounds) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Tampering | test coverage | mitigate | Tests explicitly verify _store_analysis targets backtest_analyses not ai_analyses |
| T-32-09 | Tampering | lookahead bias | mitigate | Tests verify all context queries include date <= as_of_date bound |
</threat_model>

<verification>
1. `cd backend && python -m pytest tests/test_backtest_models.py tests/test_backtest_api.py tests/test_backtest_engine.py tests/test_backtest_analysis_service.py -x -v` — all backtest tests pass
2. `cd backend && python -m pytest tests/ -x --timeout=120` — full suite (560+ tests) passes with no regressions
3. All 10 requirements (BT-01 through BT-06, SIM-01 through SIM-04) have at least 1 test covering them
</verification>

<success_criteria>
- BacktestAnalysisService tests verify date-awareness (no lookahead bias) and table isolation (never writes ai_analyses)
- Engine tests cover: signal→trade lifecycle, position evaluation (LONG+BEARISH), partial TP, timeout, slippage on all prices, cash tracking, equity snapshots
- Checkpoint/resume tests verify: skip completed dates, reload positions, restore cash
- Cancel tests verify: flag check per day, graceful stop with CANCELLED status
- No regressions in existing 560+ test suite
- Human verification confirms all imports, routes, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-backtest-engine-portfolio-simulation/32-03-SUMMARY.md`
</output>
