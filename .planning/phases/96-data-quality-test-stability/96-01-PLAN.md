---
phase: 96-data-quality-test-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_scheduler.py
autonomous: true
requirements:
  - DQ-01

must_haves:
  truths:
    - "pytest runs with 0 failures (all tests pass)"
    - "test_weekly_financial_crawl_calls_service passes"
  artifacts:
    - path: "backend/tests/test_scheduler.py"
      provides: "Fixed scheduler test matching current job implementation"
      contains: "CafeFFinancialCrawler"
  key_links:
    - from: "backend/tests/test_scheduler.py"
      to: "backend/app/scheduler/jobs.py"
      via: "mock matches actual import path"
      pattern: "CafeFFinancialCrawler"
---

<objective>
Fix pre-existing test failure in the scheduler test suite.

Purpose: The `test_weekly_financial_crawl_calls_service` test mocks `FinancialService` but the actual `weekly_financial_crawl` job now uses `CafeFFinancialCrawler` directly. The test must be updated to mock the correct class.
Output: All existing tests pass (`pytest` 0 failures).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/scheduler/jobs.py
@backend/tests/test_scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_weekly_financial_crawl_calls_service test</name>
  <files>backend/tests/test_scheduler.py</files>
  <action>
The test `test_weekly_financial_crawl_calls_service` currently mocks `app.scheduler.jobs.FinancialService` but the actual `weekly_financial_crawl()` function now does:
```python
from app.crawlers.cafef_financial_crawler import CafeFFinancialCrawler
cafef_crawler = CafeFFinancialCrawler(session)
result = await cafef_crawler.crawl_financials()
```

Fix the test to:
1. Mock `app.scheduler.jobs.CafeFFinancialCrawler` (imported inside the function via `from app.crawlers.cafef_financial_crawler import CafeFFinancialCrawler`)
   - Actually need to mock `app.crawlers.cafef_financial_crawler.CafeFFinancialCrawler` at the point of use inside `weekly_financial_crawl`. Since it's imported inside the function with `from app.crawlers.cafef_financial_crawler import CafeFFinancialCrawler`, patch it at that module path.
2. Make mock instance return `crawl_financials` as AsyncMock returning `{"success": 10, "failed": 0, "failed_symbols": []}`
3. Assert `CafeFFinancialCrawler` was instantiated with the session
4. Assert `crawl_financials()` was called once (no args — unlike old FinancialService which took `period="quarter"`)
5. Also update the test name/docstring to reflect the new implementation.

Additionally, run `pytest backend/tests/test_scheduler.py -x` to confirm the fix, then run `pytest backend/tests/ --tb=short -q` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_scheduler.py::TestJobFunctions::test_weekly_financial_crawl_calls_service -x -v</automated>
  </verify>
  <done>test_weekly_financial_crawl_calls_service passes; full test suite has 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm zero failures</name>
  <files>backend/tests/test_scheduler.py</files>
  <action>
Run the full pytest suite: `cd backend && python -m pytest tests/ --tb=short -q`

If any other tests fail:
- Identify root cause (likely similar stale mocks from code refactors)
- Fix them following the same pattern: align mock targets with actual imports in production code
- Do NOT skip or xfail tests — fix them properly

The goal is `pytest` exits with code 0.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ --tb=short -q</automated>
  </verify>
  <done>Full test suite runs with 0 failures, exit code 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this plan only modifies test files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | N/A | test files only | accept | No production code changes; test-only modifications |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/ --tb=short -q` exits with 0 failures
- No tests skipped or xfailed as workaround
</verification>

<success_criteria>
- pytest reports 0 failures across all backend tests
- test_weekly_financial_crawl_calls_service specifically passes
</success_criteria>

<output>
After completion, create `.planning/phases/96-data-quality-test-stability/96-01-SUMMARY.md`
</output>
