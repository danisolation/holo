---
phase: 96-data-quality-test-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_simulator_service.py
  - backend/tests/test_pick_pipeline.py
autonomous: true
requirements:
  - DQ-02
  - DQ-03

must_haves:
  truths:
    - "Simulator service has unit tests for buy, sell, fee calculation, FIFO matching"
    - "Pick pipeline has unit tests for signal filtering, format extraction, date range"
  artifacts:
    - path: "backend/tests/test_simulator_service.py"
      provides: "Unit tests for simulator service pure functions and BUY/SELL logic"
      min_lines: 120
    - path: "backend/tests/test_pick_pipeline.py"
      provides: "Unit tests for pick pipeline pure functions"
      min_lines: 80
  key_links:
    - from: "backend/tests/test_simulator_service.py"
      to: "backend/app/services/simulator_service.py"
      via: "imports and tests pure functions"
      pattern: "calculate_buy_fee|calculate_sell_fees|fifo_match"
    - from: "backend/tests/test_pick_pipeline.py"
      to: "backend/app/services/pick_service.py"
      via: "imports and tests pure functions"
      pattern: "extract_trading_plan|compute_pick_outcome"
---

<objective>
Add comprehensive unit tests for the simulator service and pick generation pipeline.

Purpose: DQ-02 requires simulator tests (buy, sell, SL/TP, fees, FIFO); DQ-03 requires pick pipeline tests (signal filter, unified format, date range). Both services have pure functions that can be tested without DB mocking.
Output: Two new test files with full coverage of business logic.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/services/simulator_service.py
@backend/app/services/pick_service.py
@backend/tests/conftest.py
</context>

<interfaces>
<!-- Key functions to test from simulator_service.py -->
From backend/app/services/simulator_service.py:
```python
BROKER_FEE_PCT = Decimal("0.15")  # 0.15% each side
SELL_TAX_PCT = Decimal("0.1")  # 0.1% sell tax

def calculate_buy_fee(price: Decimal, quantity: int) -> Decimal:
def calculate_sell_fees(price: Decimal, quantity: int) -> tuple[Decimal, Decimal]:
def fifo_match(lots: list[dict], sell_quantity: int) -> tuple[list[dict], Decimal]:
```

From backend/app/services/pick_service.py:
```python
def compute_pick_outcome(entry_price, stop_loss, take_profit_1, take_profit_2, daily_closes, max_trading_days=10) -> dict:
def compute_composite_score(trading_signal_confidence, combined_score, safety_score) -> float:
def is_affordable(capital, price) -> bool:
def compute_safety_score(atr_14, adx_14, avg_volume, current_price) -> float:
def extract_trading_plan(raw_response: dict) -> dict:
def compute_position_sizing(capital, entry_price, position_pct) -> dict:
def generate_rejection_reason(rsi, atr_pct, adx, avg_volume, composite_score) -> str:
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Simulator service unit tests</name>
  <files>backend/tests/test_simulator_service.py</files>
  <behavior>
    - calculate_buy_fee: 100_000 VND × 100 shares × 0.15% = 15,000 VND fee
    - calculate_buy_fee: 50_000 × 200 = 10M → fee 15,000
    - calculate_sell_fees: returns (broker_fee, sell_tax) — broker = 0.15%, tax = 0.1%
    - calculate_sell_fees: 100_000 × 100 → broker=15,000, tax=10,000
    - fifo_match: single lot, full sell → matches entire lot
    - fifo_match: two lots [100, 200], sell 150 → takes 100 from first, 50 from second
    - fifo_match: sells sorted by buy_date (FIFO order)
    - fifo_match: insufficient quantity raises ValueError
    - fifo_match: returns correct total_cost for P&L calculation
  </behavior>
  <action>
Create `backend/tests/test_simulator_service.py` with tests for all 3 pure functions:

1. **TestCalculateBuyFee class:**
   - Test standard calculation: `calculate_buy_fee(Decimal("100000"), 100)` = `Decimal("15000.00")` (100k×100×0.15/100)
   - Test with odd numbers: `calculate_buy_fee(Decimal("23500"), 300)` → verify rounding to 2 decimal places
   - Test small trade: `calculate_buy_fee(Decimal("10000"), 10)` = `Decimal("15.00")`

2. **TestCalculateSellFees class:**
   - Test returns tuple of (broker_fee, sell_tax)
   - `calculate_sell_fees(Decimal("100000"), 100)` → (15000.00, 10000.00)
   - Verify broker = 0.15% and tax = 0.1% independently
   - Test with small values for rounding

3. **TestFifoMatch class:**
   - Single lot, exact match: lots=[{id:1, remaining_quantity:100, buy_price:Decimal("50000"), buy_date:date(2024,1,1)}], sell 100 → matches=[(lot_id=1, quantity=100, buy_price=50000)], total_cost=5,000,000
   - Multiple lots FIFO order: lots with dates [Jan 1, Jan 5], sell quantity spans both → first lot consumed fully first
   - Partial match: sell less than available → only partial from first lot
   - Insufficient: sell more than available → raises ValueError with message
   - Total cost calculation: sum of (buy_price × matched_quantity) for each match

Import from `app.services.simulator_service` directly. Use `from decimal import Decimal` and `from datetime import date`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_simulator_service.py -v</automated>
  </verify>
  <done>All simulator pure function tests pass; covers buy fee, sell fees, and FIFO matching logic</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pick pipeline unit tests</name>
  <files>backend/tests/test_pick_pipeline.py</files>
  <behavior>
    - extract_trading_plan: unified format (entry_price at top level) extracts correctly
    - extract_trading_plan: single-direction format (trading_plan object) extracts correctly
    - extract_trading_plan: legacy dual-direction format (long_analysis.trading_plan) extracts correctly
    - compute_pick_outcome: close <= stop_loss → LOSER with correct return_pct
    - compute_pick_outcome: close >= take_profit_1 → WINNER
    - compute_pick_outcome: 10 days without trigger → EXPIRED
    - compute_pick_outcome: empty daily_closes → PENDING
    - compute_safety_score: low ATR + high ADX + high volume → high score
    - compute_safety_score: high ATR + low ADX + low volume → low score
    - compute_position_sizing: caps at 30% regardless of input
    - compute_position_sizing: aligns to 100-share lots
    - generate_rejection_reason: RSI > 70 produces Vietnamese "overbought" text
  </behavior>
  <action>
Create `backend/tests/test_pick_pipeline.py` with tests for pick pipeline pure functions:

1. **TestExtractTradingPlan class:**
   - Unified format: `{"entry_price": 50000, "stop_loss": 48000, "take_profit_1": 55000, "take_profit_2": 58000, "risk_reward_ratio": 2.5, "score": 8}` → extracts all fields correctly
   - Single-direction format: `{"trading_plan": {"entry_price": 50000, ...}, "confidence": 7}` → extracts from trading_plan
   - Legacy format: `{"long_analysis": {"trading_plan": {"entry_price": 50000, ...}, "confidence": 6}}` → extracts from nested path
   - Missing fields default gracefully (position_size_pct defaults to 10, confidence defaults to 5)

2. **TestComputePickOutcome class:**
   - Hit SL: entry=50000, SL=48000, TP=55000, closes=[(day1, 47000)] → outcome=LOSER, hit_stop_loss=True
   - Hit TP1: entry=50000, SL=48000, TP1=55000, closes=[(day1, 56000)] → outcome=WINNER, hit_take_profit_1=True
   - Hit TP2: entry=50000, SL=48000, TP1=55000, TP2=60000, closes=[(day1, 61000)] → hit_take_profit_2=True
   - Expired: entry=50000, SL=48000, TP=55000, 10 closes all between 49000-54000 → outcome=EXPIRED
   - Pending: empty list → outcome=PENDING, days_held=0
   - Date ordering: closes not sorted → function handles correctly (sorts internally)

3. **TestComputeSafetyScore class:**
   - Safe stock: atr=500, adx=35, volume=600000, price=50000 → score > 5
   - Risky stock: atr=3000, adx=10, volume=50000, price=50000 → score < 3
   - Score always 0-10 range

4. **TestComputePositionSizing class:**
   - Normal: capital=50M, entry=50000, pct=20 → shares multiple of 100, total_vnd ≤ 10M
   - Cap at 30%: capital=50M, entry=50000, pct=50 → treated as 30%
   - Minimum 1 lot: very high entry price relative to capital → shares=100

5. **TestGenerateRejectionReason class:**
   - RSI > 70 → contains "overbought"
   - ATR% > 4 → contains "Biến động cao"
   - ADX < 20 → contains "Xu hướng yếu"
   - Volume < 100k → contains "Volume quá thấp"

Import: `from app.services.pick_service import extract_trading_plan, compute_pick_outcome, compute_safety_score, compute_position_sizing, generate_rejection_reason`
Import: `from app.models.daily_pick import PickOutcome`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pick_pipeline.py -v</automated>
  </verify>
  <done>All pick pipeline pure function tests pass; covers signal format extraction, outcome computation, safety scoring, position sizing, and rejection reasons</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — test-only additions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02 | N/A | test files only | accept | No production code changes |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_simulator_service.py tests/test_pick_pipeline.py -v` all pass
- Combined: `cd backend && python -m pytest tests/ --tb=short -q` still 0 failures
</verification>

<success_criteria>
- test_simulator_service.py has ≥9 passing tests covering fee calc and FIFO
- test_pick_pipeline.py has ≥12 passing tests covering format extraction, outcome, safety, sizing, rejection
- All tests run independently (no DB required for pure function tests)
</success_criteria>

<output>
After completion, create `.planning/phases/96-data-quality-test-stability/96-02-SUMMARY.md`
</output>
