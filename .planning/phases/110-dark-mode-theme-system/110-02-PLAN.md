---
phase: 110-dark-mode-theme-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/trading-plan-panel.tsx
  - frontend/src/components/analysis-card.tsx
  - frontend/src/components/rumor-score-panel.tsx
  - frontend/src/components/discovery-table.tsx
  - frontend/src/components/watchlist-table.tsx
  - frontend/src/components/support-resistance-card.tsx
  - frontend/src/components/heatmap.tsx
  - frontend/src/components/bid-ask-panel.tsx
  - frontend/src/components/indicator-chart.tsx
  - frontend/src/components/pipeline-timeline.tsx
  - frontend/src/components/error-rate-chart.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/ticker/[symbol]/page.tsx
  - frontend/src/components/market/ad-line-chart.tsx
  - frontend/src/components/market/flow-tab.tsx
  - frontend/src/components/market/highs-lows-chart.tsx
  - frontend/src/components/market/ma-breadth-chart.tsx
  - frontend/src/components/market/peer-comparison-section.tsx
  - frontend/src/components/market/peer-radar-chart.tsx
  - frontend/src/components/market/screener-table.tsx
  - frontend/src/components/market/sector-detail-table.tsx
  - frontend/src/components/market/sector-drilldown.tsx
  - frontend/src/components/market/sector-performance-chart.tsx
  - frontend/src/components/market/sector-radar.tsx
  - frontend/src/components/market/sector-ranking.tsx
  - frontend/src/components/simulator/ai-accuracy-panel.tsx
  - frontend/src/components/simulator/comparison-section.tsx
  - frontend/src/components/simulator/equity-chart.tsx
  - frontend/src/components/simulator/pending-signals.tsx
  - frontend/src/components/simulator/portfolio-summary.tsx
  - frontend/src/components/simulator/positions-table.tsx
  - frontend/src/components/simulator/trade-form.tsx
  - frontend/src/components/simulator/trade-history.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "All components use Tailwind CSS variable classes instead of hardcoded hex for theme-dependent colors"
    - "Trading bull/bear colors use text-trading-bull / text-trading-bear Tailwind classes"
    - "All pages render with readable contrast in both light and dark mode"
  artifacts:
    - path: "frontend/src/components/trading-plan-panel.tsx"
      provides: "Theme-aware trading plan with CSS var colors"
      contains: "trading-bull"
    - path: "frontend/src/components/analysis-card.tsx"
      provides: "Theme-aware analysis card"
      contains: "trading-bull"
    - path: "frontend/src/app/page.tsx"
      provides: "Theme-aware homepage"
      contains: "trading-bull"
  key_links:
    - from: "frontend/src/components/*.tsx"
      to: "frontend/src/app/globals.css"
      via: "CSS variable classes (text-trading-bull, bg-trading-bull-bg)"
      pattern: "trading-bull|trading-bear"
---

<objective>
Replace hardcoded hex colors across all components with CSS variable-based Tailwind classes.

Purpose: Ensure all dashboard pages render correctly in both light and dark mode without broken colors or poor contrast.
Output: All 33 component files use semantic CSS classes for trading colors instead of hardcoded hex values.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/110-dark-mode-theme-system/110-CONTEXT.md
@frontend/src/app/globals.css
@frontend/src/components/trading-plan-panel.tsx
@frontend/src/components/analysis-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded trading colors with CSS variable classes in all components</name>
  <files>frontend/src/components/trading-plan-panel.tsx, frontend/src/components/analysis-card.tsx, frontend/src/components/rumor-score-panel.tsx, frontend/src/components/discovery-table.tsx, frontend/src/components/watchlist-table.tsx, frontend/src/components/support-resistance-card.tsx, frontend/src/components/heatmap.tsx, frontend/src/components/bid-ask-panel.tsx, frontend/src/components/indicator-chart.tsx, frontend/src/components/pipeline-timeline.tsx, frontend/src/components/error-rate-chart.tsx, frontend/src/app/page.tsx, frontend/src/app/ticker/[symbol]/page.tsx</files>
  <action>
Perform a systematic find-and-replace across ALL listed files. The pattern is:

**Tailwind class replacements (in className strings):**
- `text-[#26a69a]` → `text-trading-bull`
- `text-[#ef5350]` → `text-trading-bear`
- `bg-[#26a69a]/10` → `bg-trading-bull-bg`
- `bg-[#ef5350]/10` → `bg-trading-bear-bg`
- `bg-[#26a69a]/5` → `bg-trading-bull-bg`
- `bg-[#ef5350]/5` → `bg-trading-bear-bg`
- `border-[#26a69a]` → `border-trading-bull`
- `border-[#ef5350]` → `border-trading-bear`
- `border-[#ef5350]/20` → `border-trading-bear/20`

**For inline style colors (e.g., in Recharts/lightweight-charts JS objects):**
- Keep `#26a69a` and `#ef5350` in JS config objects that pass to chart libraries (Recharts, lightweight-charts) — these don't use Tailwind and the colors work fine in both themes since charts render on their own backgrounds.

**For `bg-[#0f172a]` occurrences (dark-only background):**
- Replace with `bg-card` or `bg-muted` (these use CSS vars that auto-switch).

**Files to check (run grep to find all occurrences):**
```bash
grep -rn "#26a69a\|#ef5350\|#0f172a" frontend/src/components/ frontend/src/app/ --include="*.tsx"
```

Process each file systematically. Focus on Tailwind class strings (className=). Leave JS chart config objects alone (they don't participate in CSS theming).

**Important edge case:** In `indicator-chart.tsx` and Recharts-based charts, colors passed as props to `<Line stroke="#2196F3">` etc. stay as-is — these are rendered on SVG and the colors work in both themes.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | Select-String -Pattern "error|Error" -NotMatch "Linting" | Select-Object -First 5; echo "EXIT: $LASTEXITCODE"</automated>
  </verify>
  <done>No more hardcoded hex in Tailwind className strings for trading colors; all use text-trading-bull/bear and bg-trading-bull-bg/bear-bg; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Fix market and simulator component colors</name>
  <files>frontend/src/components/market/ad-line-chart.tsx, frontend/src/components/market/flow-tab.tsx, frontend/src/components/market/highs-lows-chart.tsx, frontend/src/components/market/ma-breadth-chart.tsx, frontend/src/components/market/peer-comparison-section.tsx, frontend/src/components/market/peer-radar-chart.tsx, frontend/src/components/market/screener-table.tsx, frontend/src/components/market/sector-detail-table.tsx, frontend/src/components/market/sector-drilldown.tsx, frontend/src/components/market/sector-performance-chart.tsx, frontend/src/components/market/sector-radar.tsx, frontend/src/components/market/sector-ranking.tsx, frontend/src/components/simulator/ai-accuracy-panel.tsx, frontend/src/components/simulator/comparison-section.tsx, frontend/src/components/simulator/equity-chart.tsx, frontend/src/components/simulator/pending-signals.tsx, frontend/src/components/simulator/portfolio-summary.tsx, frontend/src/components/simulator/positions-table.tsx, frontend/src/components/simulator/trade-form.tsx, frontend/src/components/simulator/trade-history.tsx</files>
  <action>
Same replacement pattern as Task 1 for market/ and simulator/ subdirectories:

**Tailwind class replacements:**
- `text-[#26a69a]` → `text-trading-bull`
- `text-[#ef5350]` → `text-trading-bear`
- `bg-[#26a69a]...` → `bg-trading-bull-bg`
- `bg-[#ef5350]...` → `bg-trading-bear-bg`
- `border-[#26a69a]` → `border-trading-bull`
- `border-[#ef5350]` → `border-trading-bear`

**For Recharts/chart library props (stroke, fill, color in JS objects):**
- Keep hardcoded hex values — these are passed to SVG rendering and work in both themes.
- Example: `<Bar fill="#26a69a">` stays as-is (SVG renders its own colors on chart background).

**For any `bg-[#0f172a]` or `bg-[#1e293b]` used as chart containers:**
- Replace with `bg-card` or `bg-muted`.

After all replacements, run:
```bash
grep -rn "className.*#[0-9a-fA-F]" frontend/src/components/ frontend/src/app/ --include="*.tsx"
```
This should return ZERO results (all Tailwind class hex colors eliminated). Any remaining `#hex` should only be in JS config objects passed to chart libraries.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | Select-String -Pattern "error|Error" -NotMatch "Linting" | Select-Object -First 5; echo "EXIT: $LASTEXITCODE"</automated>
  </verify>
  <done>All market/ and simulator/ components use CSS variable classes for Tailwind; only chart library JS configs retain hardcoded hex; build passes; grep className.*#hex returns zero matches</done>
</task>

<task type="auto">
  <name>Task 3: Verify dark/light mode rendering across all pages</name>
  <files>frontend/src/app/providers.tsx</files>
  <action>
Run a final verification pass:

1. Run `cd frontend && npx next build` — must pass cleanly (exit 0).

2. Grep audit — confirm NO hardcoded hex in Tailwind classes:
```bash
grep -rn "className.*\[#" frontend/src/components/ frontend/src/app/ --include="*.tsx" | grep -v "// " | head -20
```
Expected: Zero results OR only chart-specific inline styles (style={{ }}) which are acceptable.

3. Verify providers.tsx has `defaultTheme="system"` and `enableSystem={true}` (from Plan 01).

4. Verify globals.css has `--trading-bull` and `--chart-bg` in both `:root` and `.dark` (from Plan 01).

If any issues found during grep audit, fix them inline following the same pattern (replace with CSS variable class).
  </action>
  <verify>
    <automated>cd frontend && npx next build && echo "BUILD_OK"</automated>
  </verify>
  <done>Build passes; no hardcoded hex in Tailwind className strings; all pages ready for both light and dark mode</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure cosmetic changes, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-02 | I (Info Disclosure) | CSS variable names | accept | Color variable names reveal no sensitive info |
</threat_model>

<verification>
- `cd frontend && npx next build` completes exit 0
- `grep -rn "className.*\[#" frontend/src/ --include="*.tsx"` returns zero matches
- All trading green/red colors use text-trading-bull/bear CSS classes
- Chart library JS configs still use hex (acceptable — they render on canvas/SVG)
</verification>

<success_criteria>
- Zero hardcoded hex colors in Tailwind className strings across all components
- All 33 files identified in audit use CSS variable-based classes
- Frontend builds successfully
- Trading bull (#26a69a) and bear (#ef5350) semantic colors accessible via CSS vars
</success_criteria>

<output>
After completion, create `.planning/phases/110-dark-mode-theme-system/110-02-SUMMARY.md`
</output>
