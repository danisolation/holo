---
phase: 35-database-model-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/015_remove_price_alerts_and_source.py
  - backend/app/models/price_alert.py
  - backend/app/models/__init__.py
  - backend/app/models/news_article.py
  - backend/app/crawlers/cafef_crawler.py
  - backend/app/telegram/handlers.py
  - backend/app/telegram/services.py
  - backend/app/telegram/formatter.py
  - backend/app/scheduler/jobs.py
  - backend/app/scheduler/manager.py
  - backend/tests/test_telegram.py
  - backend/tests/test_scheduler.py
  - backend/tests/test_cafef_crawler.py
autonomous: true
requirements: [CLN-01, CLN-04]

must_haves:
  truths:
    - "The price_alerts table no longer exists in the database schema"
    - "No Python file in backend/app imports or references PriceAlert"
    - "The /alert Telegram command is no longer registered"
    - "The daily_price_alert_check scheduler job no longer exists"
    - "The news_article model no longer has a source column"
    - "The cafef crawler no longer inserts a source field"
  artifacts:
    - path: "backend/alembic/versions/015_remove_price_alerts_and_source.py"
      provides: "Alembic migration dropping price_alerts table and news_articles.source column"
      contains: "drop_table"
    - path: "backend/app/models/__init__.py"
      provides: "Model registry without PriceAlert"
  key_links:
    - from: "backend/app/telegram/handlers.py"
      to: "backend/app/models/__init__.py"
      via: "import removal"
      pattern: "No PriceAlert import"
    - from: "backend/app/scheduler/manager.py"
      to: "backend/app/scheduler/jobs.py"
      via: "job chaining removal"
      pattern: "No daily_price_alert_check"
---

<objective>
Remove the price_alerts table entirely and the news_article.source column — cleaning up all model definitions, service references, handler imports, scheduler jobs, and tests.

Purpose: Eliminate dead database objects (CLN-01: price_alert table unused, CLN-04: news_article.source always "cafef") so the data model reflects only what the system actually uses.
Output: Alembic migration, cleaned model files, updated services/handlers/scheduler/tests with zero references to removed entities.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-database-model-cleanup/35-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/models/__init__.py (current):
```python
from app.models.price_alert import PriceAlert  # noqa: E402
# In __all__: "PriceAlert"
```

From backend/app/models/news_article.py (current):
```python
source: Mapped[str] = mapped_column(
    String(20), nullable=False, server_default="cafef"
)
```

From backend/app/telegram/handlers.py:
```python
from app.models.price_alert import PriceAlert
# register_handlers includes: app.add_handler(CommandHandler("alert", alert_command))
```

From backend/app/scheduler/manager.py:
```python
"daily_price_alert_check_triggered": "Daily Price Alert Check",
# _on_job_executed chains daily_price_alert_check after daily_price_crawl_upcom
```

From backend/alembic/versions/014_backtest_tables.py:
```python
revision: str = "014"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration and update model files</name>
  <files>
    backend/alembic/versions/015_remove_price_alerts_and_source.py,
    backend/app/models/price_alert.py,
    backend/app/models/__init__.py,
    backend/app/models/news_article.py,
    backend/app/crawlers/cafef_crawler.py,
    backend/app/telegram/formatter.py
  </files>
  <read_first>
    backend/alembic/versions/014_backtest_tables.py,
    backend/app/models/price_alert.py,
    backend/app/models/__init__.py,
    backend/app/models/news_article.py,
    backend/app/crawlers/cafef_crawler.py,
    backend/app/telegram/formatter.py
  </read_first>
  <action>
    1. Create `backend/alembic/versions/015_remove_price_alerts_and_source.py`:
       - revision = "015", down_revision = "014"
       - upgrade(): `op.drop_table("price_alerts")` then `op.drop_column("news_articles", "source")`
       - downgrade(): Recreate price_alerts table with columns (id BigInteger PK, chat_id String(50), ticker_id Integer FK tickers.id, target_price Numeric(12,2), direction String(10), is_triggered Boolean default false, created_at TIMESTAMP(tz) server_default now(), triggered_at TIMESTAMP(tz) nullable). Add source column back: `op.add_column("news_articles", sa.Column("source", sa.String(20), nullable=False, server_default="cafef"))`

    2. DELETE file `backend/app/models/price_alert.py` entirely.

    3. Edit `backend/app/models/__init__.py`:
       - Remove the line: `from app.models.price_alert import PriceAlert  # noqa: E402`
       - Remove `"PriceAlert"` from the `__all__` list

    4. Edit `backend/app/models/news_article.py`:
       - Remove the `source` field (lines 28-30): Remove `source: Mapped[str] = mapped_column(String(20), nullable=False, server_default="cafef")`
       - Remove `String` from the sqlalchemy import if no longer needed (check if String is used by `url` field — it IS, so keep String in imports)

    5. Edit `backend/app/crawlers/cafef_crawler.py`:
       - In `_parse_articles` method (~line 158), remove `"source": "cafef"` from the article dict
       - In `_store_articles` method (~line 182), remove `source=article["source"]` from the insert values

    6. Edit `backend/app/telegram/formatter.py`:
       - Remove `alert_created` static method (lines 139-144)
       - Remove `alert_triggered` static method (lines 146-155)
       - In `welcome()` method, remove the line: `"/alert &lt;mã&gt; &lt;giá&gt; — Đặt cảnh báo giá\n"` (line 31)
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models import Base; assert not hasattr(Base, 'metadata') or 'price_alerts' not in [t.name for t in Base.metadata.sorted_tables if t.name == 'price_alerts']; print('PriceAlert model removed OK')" && python -c "from app.models.news_article import NewsArticle; assert not hasattr(NewsArticle, 'source'); print('NewsArticle.source removed OK')" && python -c "from app.models import __all__ as a; assert 'PriceAlert' not in a; print('__all__ cleaned OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/models/price_alert.py` does NOT exist
    - `grep -r "PriceAlert" backend/app/models/` returns no results
    - `grep -r "price_alert" backend/app/models/` returns no results
    - `grep "source" backend/app/models/news_article.py` returns no lines containing a `source` mapped_column
    - `grep "source" backend/app/crawlers/cafef_crawler.py` returns no lines with `"source": "cafef"` or `source=article`
    - `grep "alert_created\|alert_triggered" backend/app/telegram/formatter.py` returns no results
    - `grep "alert.*giá" backend/app/telegram/formatter.py` returns no results
    - File `backend/alembic/versions/015_remove_price_alerts_and_source.py` exists with `revision = "015"` and `down_revision = "014"`
  </acceptance_criteria>
  <done>Alembic migration 015 created. PriceAlert model deleted. news_article.source removed from model. cafef_crawler no longer references source field. Formatter alert methods removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove PriceAlert from handlers, services, scheduler, and tests</name>
  <files>
    backend/app/telegram/handlers.py,
    backend/app/telegram/services.py,
    backend/app/scheduler/jobs.py,
    backend/app/scheduler/manager.py,
    backend/tests/test_telegram.py,
    backend/tests/test_scheduler.py,
    backend/tests/test_cafef_crawler.py
  </files>
  <read_first>
    backend/app/telegram/handlers.py,
    backend/app/telegram/services.py,
    backend/app/scheduler/jobs.py,
    backend/app/scheduler/manager.py,
    backend/tests/test_telegram.py,
    backend/tests/test_scheduler.py,
    backend/tests/test_cafef_crawler.py
  </read_first>
  <action>
    1. Edit `backend/app/telegram/handlers.py`:
       - Remove the import: `from app.models.price_alert import PriceAlert`
       - Remove `app.add_handler(CommandHandler("alert", alert_command))` from `register_handlers()`
       - Remove the entire `alert_command` function (lines 260-322)

    2. Edit `backend/app/telegram/services.py`:
       - Remove the import: `from app.models.price_alert import PriceAlert`
       - Remove the entire `check_price_alerts` method from the `AlertService` class (lines 113-195). This method references PriceAlert model extensively.

    3. Edit `backend/app/scheduler/jobs.py`:
       - Remove the entire `daily_price_alert_check` function (lines 514-540). This function calls `service.check_price_alerts()`.

    4. Edit `backend/app/scheduler/manager.py`:
       - Remove `"daily_price_alert_check_triggered": "Daily Price Alert Check",` from the `_JOB_NAMES` dict (line 43)
       - In the `_on_job_executed` function, inside the `event.job_id == "daily_price_crawl_upcom"` block (lines 98-106), remove the entire price alert check chaining block:
         ```python
         # Also trigger price alert check (parallel branch)
         from app.scheduler.jobs import daily_price_alert_check
         logger.info("Chaining: daily_price_crawl_upcom → daily_price_alert_check")
         scheduler.add_job(
             daily_price_alert_check,
             id="daily_price_alert_check_triggered",
             replace_existing=True,
             misfire_grace_time=3600,
         )
         ```
       - Update the logger.info chain description string (~line 347) to remove `+ [price_alerts]` from the description. Change `"daily_price_crawl_upcom → [indicators → AI → news → sentiment → combined → trading_signal → signal_alerts + hnx_upcom_analysis] + [price_alerts] + [corporate_action_check → exdate_alert_check], "` to `"daily_price_crawl_upcom → [indicators → AI → news → sentiment → combined → trading_signal → signal_alerts + hnx_upcom_analysis] + [corporate_action_check → exdate_alert_check], "`

    5. Edit `backend/tests/test_telegram.py`:
       - Remove the `test_check_price_alerts_no_chat_id` test method (lines 150-160)
       - Remove the `test_price_crawl_chains_to_both_indicators_and_price_alerts` test method (lines 279-293)

    6. Edit `backend/tests/test_scheduler.py`:
       - In `test_on_job_executed_chains_indicators_after_price_crawl` (line 165):
         - Update the docstring: change "must trigger daily_indicator_compute and daily_price_alert_check" to "must trigger daily_indicator_compute and daily_corporate_action_check"
         - Change `assert mock_add.call_count == 4` to `assert mock_add.call_count == 3`
         - Remove `assert "daily_price_alert_check_triggered" in call_ids`

    7. Edit `backend/tests/test_cafef_crawler.py`:
       - In `test_parse_articles_returns_valid_article` (~line 29), remove the assertion: `assert articles[0]["source"] == "cafef"`
  </action>
  <verify>
    <automated>cd backend && python -c "import ast, sys; [sys.exit(1) for f in ['app/telegram/handlers.py','app/telegram/services.py','app/scheduler/jobs.py','app/scheduler/manager.py'] if 'PriceAlert' in open(f).read() or 'price_alert' in open(f).read().lower().replace('price_alert_check','').replace('price_alerts','')]; print('No PriceAlert refs')" && python -m pytest tests/test_telegram.py tests/test_scheduler.py tests/test_cafef_crawler.py -x --tb=short -q 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "PriceAlert" backend/app/telegram/` returns no results
    - `grep -r "price_alert" backend/app/telegram/` returns no results
    - `grep "daily_price_alert_check" backend/app/scheduler/jobs.py` returns no results
    - `grep "daily_price_alert_check" backend/app/scheduler/manager.py` returns no results
    - `grep "alert_command" backend/app/telegram/handlers.py` returns no results
    - `grep "check_price_alerts" backend/app/telegram/services.py` returns no results
    - `grep "price_alert" backend/tests/test_telegram.py` returns no results
    - `grep "price_alert" backend/tests/test_scheduler.py` returns no results (except possibly in unrelated context)
    - `grep 'source.*cafef' backend/tests/test_cafef_crawler.py` returns no results
    - In test_scheduler.py, the chain test asserts `mock_add.call_count == 3` (not 4)
    - `python -m pytest tests/test_telegram.py tests/test_scheduler.py tests/test_cafef_crawler.py -x` passes
  </acceptance_criteria>
  <done>All PriceAlert references removed from handlers, services, scheduler, and tests. news_article.source assertion removed from cafef crawler test. Scheduler chain count updated from 4 to 3. All affected tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → PostgreSQL | Schema changes via migration — irreversible in production without backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | T (Tampering) | Alembic migration 015 | mitigate | Migration has downgrade() that recreates price_alerts and adds source column back. Test on dev DB first per STATE.md blocker note. |
| T-35-02 | D (Denial of Service) | Scheduler chain | accept | Removing one parallel branch (price_alert_check) from job chain — remaining chains unaffected. Low risk. |
</threat_model>

<verification>
1. `grep -rn "PriceAlert\|price_alert" backend/app/ backend/tests/` returns ZERO results (excluding migration file and any unrelated "price" references)
2. `grep -n "\.source" backend/app/models/news_article.py` returns no mapped_column source field
3. `python -m pytest backend/tests/test_telegram.py backend/tests/test_scheduler.py backend/tests/test_cafef_crawler.py -x` all pass
4. File `backend/app/models/price_alert.py` does NOT exist
5. File `backend/alembic/versions/015_remove_price_alerts_and_source.py` exists
</verification>

<success_criteria>
- price_alerts table is marked for removal via Alembic migration 015
- PriceAlert model file is deleted
- Zero references to PriceAlert remain in backend/app/ or backend/tests/
- news_article.source column is marked for removal in migration 015
- NewsArticle model no longer defines a source field
- cafef_crawler no longer passes source when inserting articles
- /alert command is unregistered from Telegram bot
- daily_price_alert_check scheduler job is removed
- Job chain from price_crawl_upcom chains to 3 jobs (not 4)
- All affected tests updated and passing
</success_criteria>

<output>
After completion, create `.planning/phases/35-database-model-cleanup/35-01-SUMMARY.md`
</output>
