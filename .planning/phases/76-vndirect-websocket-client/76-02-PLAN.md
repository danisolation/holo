---
phase: 76-vndirect-websocket-client
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - backend/app/services/realtime_price_service.py
  - backend/app/scheduler/manager.py
  - backend/app/main.py
  - backend/app/config.py
autonomous: true
requirements: [WS-01, WS-03, WS-04]

must_haves:
  truths:
    - "VNDirect WS client starts as asyncio background task on app startup"
    - "Price updates from VNDirect flow into ConnectionManager.broadcast()"
    - "VCI polling is replaced by WebSocket as primary data source"
    - "Client lifecycle tied to FastAPI app (starts on startup, stops on shutdown)"
  artifacts:
    - path: "backend/app/services/realtime_price_service.py"
      provides: "Updated service that launches VNDirect WS client instead of VCI polling"
      contains: "VNDirectWSClient"
    - path: "backend/app/main.py"
      provides: "Startup/shutdown hooks for VNDirect WS client"
      contains: "vndirect"
  key_links:
    - from: "backend/app/services/vndirect_ws_client.py"
      to: "backend/app/ws/prices.py"
      via: "on_price_update callback → connection_manager.broadcast()"
      pattern: "connection_manager\\.broadcast"
    - from: "backend/app/main.py"
      to: "backend/app/services/vndirect_ws_client.py"
      via: "asyncio.create_task on startup event"
      pattern: "create_task.*vndirect"
---

<objective>
Integrate VNDirect WebSocket client into the existing application lifecycle, replacing VCI polling as the real-time data source.

Purpose: Wire the new WS client into the app so price updates flow to frontend clients automatically during market hours.
Output: Updated startup/shutdown hooks, removed VCI polling job for real-time, VNDirect WS as primary source.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-vndirect-websocket-client/76-01-SUMMARY.md
@backend/app/services/realtime_price_service.py
@backend/app/ws/prices.py
@backend/app/scheduler/manager.py
@backend/app/main.py
@backend/app/config.py

<interfaces>
From 76-01 (VNDirectWSClient):
```python
from app.services.vndirect_ws_client import VNDirectWSClient, parse_sp_message, parse_ba_message

class VNDirectWSClient:
    def __init__(self, symbols: list[str], on_price_update: Callable, on_bid_ask_update: Callable | None = None): ...
    async def start(self) -> None: ...  # Long-running loop (market hours + reconnect)
    async def stop(self) -> None: ...   # Graceful shutdown
```

From backend/app/ws/prices.py:
```python
connection_manager = ConnectionManager()  # module-level singleton
async def broadcast(self, prices: dict[str, dict]) -> None: ...
```

From backend/app/services/realtime_price_service.py:
```python
class RealtimePriceService:
    def __init__(self, crawler, connection_manager, exchange_map=None): ...
    async def poll_and_broadcast(self) -> None: ...
    def get_latest_prices(self, symbols: list[str]) -> dict[str, dict]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire VNDirect WS client into app startup and update RealtimePriceService</name>
  <files>backend/app/services/realtime_price_service.py, backend/app/config.py</files>
  <action>
1. **Add config settings** in `backend/app/config.py`:
   - `vndirect_ws_url: str = "wss://price-cmc-04.vndirect.com.vn/realtime/websocket"` — configurable endpoint
   - `vndirect_ws_enabled: bool = True` — feature flag to fall back to VCI polling

2. **Update `backend/app/services/realtime_price_service.py`**:
   - Add method `async def handle_ws_price_update(self, prices: dict[str, dict]) -> None:` — receives parsed prices from VNDirect WS client, updates `_latest_prices` cache, calls `self._connection_manager.broadcast(changed)` (same diff-detection logic as poll_and_broadcast).
   - Add method `async def handle_ws_bid_ask_update(self, bid_asks: dict[str, dict]) -> None:` — receives parsed bid/ask data, broadcasts as `{"type": "bid_ask_update", "data": {...}}` to relevant subscribers.
   - Add method `def get_watchlist_symbols(self) -> list[str]:` — returns list of all currently subscribed symbols from connection_manager (used to tell VNDirect WS what to subscribe to).
   - Keep existing `poll_and_broadcast()` intact as fallback if `vndirect_ws_enabled=False`.
   - Update module-level singleton factory to support both modes.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.services.realtime_price_service import RealtimePriceService; print('OK')"</automated>
  </verify>
  <done>
    - RealtimePriceService has handle_ws_price_update and handle_ws_bid_ask_update methods
    - Config has vndirect_ws_url and vndirect_ws_enabled settings
    - Existing poll_and_broadcast preserved as fallback
  </done>
</task>

<task type="auto">
  <name>Task 2: Add startup/shutdown lifecycle hooks for VNDirect WS client</name>
  <files>backend/app/main.py, backend/app/scheduler/manager.py</files>
  <action>
1. **Update `backend/app/main.py`** (FastAPI lifespan or startup/shutdown events):
   - On startup, if `settings.vndirect_ws_enabled`:
     - Get watchlist symbols from DB (query UserWatchlist for all distinct symbols)
     - Create `VNDirectWSClient(symbols=symbols, on_price_update=realtime_svc.handle_ws_price_update, on_bid_ask_update=realtime_svc.handle_ws_bid_ask_update)`
     - Launch `asyncio.create_task(client.start())` — store task reference for shutdown
     - Do NOT add the `realtime_price_poll` interval job to scheduler (skip it)
   - If `settings.vndirect_ws_enabled` is False, keep existing VCI polling behavior unchanged
   - On shutdown: call `client.stop()` and cancel the task

2. **Update `backend/app/scheduler/manager.py`**:
   - Wrap the `realtime_price_poll` and `realtime_heartbeat` job registration in a condition: only add if `settings.vndirect_ws_enabled` is False.
   - This ensures no conflict between WS client and polling when WS is active.

The VNDirect WS client handles its own market hours logic internally (sleeps outside market hours, reconnects during market hours), so no APScheduler involvement needed for the WS path.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.main import app; print('FastAPI app loads OK')"</automated>
  </verify>
  <done>
    - VNDirect WS client launches as background task on app startup when enabled
    - VCI polling jobs are skipped when VNDirect WS is enabled
    - Graceful shutdown stops the WS client
    - Feature flag allows instant rollback to VCI polling
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VNDirect WS → price cache | External data updates internal state used for broadcasting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04 | Spoofing | handle_ws_price_update | accept | VNDirect is the sole data source; no alternative to verify against in real-time; prices are display-only |
| T-76-05 | Denial of Service | startup lifecycle | mitigate | Feature flag `vndirect_ws_enabled` allows instant fallback to VCI polling if WS endpoint goes down permanently |
| T-76-06 | Elevation of Privilege | asyncio.create_task | accept | Task runs within same process; no privilege boundary crossed |
</threat_model>

<verification>
- `cd backend && python -c "from app.main import app; print('OK')"` — app loads without errors
- `cd backend && python -c "from app.config import settings; assert hasattr(settings, 'vndirect_ws_url'); print('config OK')"` — new settings exist
- `cd backend && python -m pytest tests/test_vndirect_ws_client.py -v` — existing tests still pass
</verification>

<success_criteria>
- VNDirect WS client is the primary real-time data source when enabled
- Price updates flow: VNDirect WS → parse → RealtimePriceService → ConnectionManager → frontend clients
- Feature flag allows instant rollback to VCI polling
- App starts and stops cleanly with WS client lifecycle managed
</success_criteria>

<output>
After completion, create `.planning/phases/76-vndirect-websocket-client/76-02-SUMMARY.md`
</output>
