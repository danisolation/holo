# Plan 111-01: Loading Skeletons & Mobile Fixes

## Phase
111 — Mobile Responsive & Loading States

## Goal
Add route-level loading skeletons and fix remaining mobile responsiveness gaps.

## Requirements
- UX-02: Dashboard displays correctly on mobile
- UX-04: Loading skeleton and smooth route transitions

## Tasks

### Task 1: Add loading.tsx skeleton files for all routes
**Files**: Create loading.tsx in each app route directory
- `app/loading.tsx` (root)
- `app/dashboard/loading.tsx`
- `app/market/loading.tsx`
- `app/market/screener/loading.tsx`
- `app/discovery/loading.tsx`
- `app/simulator/loading.tsx`
- `app/watchlist/loading.tsx`
- `app/ticker/[symbol]/loading.tsx`
- `app/dashboard/health/loading.tsx`

Each should render appropriate skeleton layout matching the page structure.

### Task 2: Fix screener table mobile responsiveness
**File**: `components/market/screener-table.tsx`
- Add overflow-x-auto wrapper
- Add min-width on table
- Hide less-important columns on mobile (md:table-cell)

### Task 3: Fix ticker page header responsive layout
**File**: `app/ticker/[symbol]/page.tsx`
- Wrap header area for mobile stacking
- Ensure action buttons don't overflow

## Verification
- `npx next build` passes
- All loading.tsx files render valid JSX
