---
phase: 09-ai-prompt-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/ai_analysis_service.py
autonomous: true
requirements: [AI-07, AI-08, AI-09, AI-11, AI-13]

must_haves:
  truths:
    - "Gemini API calls receive persona+rubric via system_instruction parameter, not embedded in user prompt"
    - "Each analysis type has 1-2 few-shot examples in the user prompt showing expected input→output"
    - "Scoring rubric with anchors 1-2 through 9-10 appears in every system instruction"
    - "Technical and fundamental use English system instructions; sentiment and combined use Vietnamese"
    - "Each analysis type uses its own temperature (tech=0.1, fund=0.2, sentiment=0.3, combined=0.2)"
  artifacts:
    - path: "backend/app/services/ai_analysis_service.py"
      provides: "Refactored prompts with system_instruction separation, few-shot, rubric, language, temperature"
      contains: "SCORING_RUBRIC"
  key_links:
    - from: "ANALYSIS_TEMPERATURES dict"
      to: "_call_gemini_with_retry"
      via: "temperature parameter"
      pattern: "temperature=temperature"
    - from: "*_SYSTEM_INSTRUCTION constants"
      to: "GenerateContentConfig"
      via: "system_instruction parameter"
      pattern: "system_instruction=system_instruction"
    - from: "_analyze_*_batch methods"
      to: "_call_gemini"
      via: "passes type-specific temp + system_instruction"
      pattern: "ANALYSIS_TEMPERATURES\\[AnalysisType"
---

<objective>
Refactor AI prompt architecture: system instruction separation, scoring rubric, per-type temperature, few-shot examples, and language consistency.

Purpose: Move persona/rubric/rules from user prompts into Gemini's `system_instruction` parameter for better model adherence. Add few-shot examples and explicit scoring rubric to reduce score clustering. Tune temperature per analysis type. Enforce language consistency.

Output: Refactored `ai_analysis_service.py` with constants, updated API call signatures, rebuilt prompt builders, and wired batch analyzers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ai-prompt-improvements/09-CONTEXT.md
@.planning/phases/09-ai-prompt-improvements/09-RESEARCH.md
@backend/app/services/ai_analysis_service.py
@backend/app/models/ai_analysis.py
@backend/tests/test_ai_analysis_service.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/models/ai_analysis.py:
```python
class AnalysisType(str, enum.Enum):
    TECHNICAL = "technical"
    FUNDAMENTAL = "fundamental"
    SENTIMENT = "sentiment"
    COMBINED = "combined"
```

Current _call_gemini_with_retry signature (line 414):
```python
async def _call_gemini_with_retry(self, prompt: str, response_schema):
```

Current _call_gemini signature (line 435):
```python
async def _call_gemini(self, prompt: str, response_schema):
```

Current GenerateContentConfig usage (line 425-431):
```python
config=types.GenerateContentConfig(
    response_mime_type="application/json",
    response_schema=response_schema,
    temperature=0.2,  # hardcoded
    max_output_tokens=16384,
    thinking_config=thinking_config,
)
```

SDK supports (verified from google-genai v1.73.1):
```python
config=types.GenerateContentConfig(
    system_instruction="...",  # string accepted
    response_mime_type="application/json",
    response_schema=response_schema,
    temperature=0.1,
    max_output_tokens=16384,
    thinking_config=thinking_config,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define constants and refactor Gemini API call signatures</name>
  <files>backend/app/services/ai_analysis_service.py</files>
  <action>
**Add module-level constants** after the existing `_gemini_lock` definition (around line 48):

1. **SCORING_RUBRIC** constant (shared across all system instructions, per D-09-03):
```
Scoring rubric (apply consistently):
- 1-2: Very weak signal / very negative outlook
- 3-4: Weak signal / slightly negative outlook
- 5-6: Moderate / neutral — no clear direction
- 7-8: Strong signal / positive outlook
- 9-10: Very strong signal / very positive outlook
Use the FULL range. Scores of 1-2 and 9-10 are valid for extreme cases.
```

2. **4 system instruction constants** (per D-09-01, D-09-05). Each combines persona (1-2 sentences) + output format rules + SCORING_RUBRIC + language rule. Keep concise per Pitfall 1 (thinking budget). Use string concatenation with SCORING_RUBRIC.

   - `TECHNICAL_SYSTEM_INSTRUCTION` (English per D-09-05): Senior HOSE technical analyst persona. Output format: signal (strong_buy/buy/neutral/sell/strong_sell), strength (1-10), reasoning (2-3 sentences English). Include guidance on RSI zones, MACD crossovers, price vs MAs, Bollinger Bands.
   - `FUNDAMENTAL_SYSTEM_INSTRUCTION` (English per D-09-05): Senior HOSE fundamental analyst persona. Output format: health (strong/good/neutral/weak/critical), score (1-10), reasoning (2-3 sentences English). Include guidance on VN market P/E ~12-15, ROE, ROA, growth, stability.
   - `SENTIMENT_SYSTEM_INSTRUCTION` (Vietnamese per D-09-05): HOSE market sentiment expert persona. Output format: sentiment (very_positive/positive/neutral/negative/very_negative), score (1-10), reasoning (2-3 câu tiếng Việt). Include rule: no news = neutral, score = 5.
   - `COMBINED_SYSTEM_INSTRUCTION` (Vietnamese per D-09-05): HOSE investment advisor persona. Output format: recommendation (mua/ban/giu), confidence (1-10), explanation (tiếng Việt, max 200 từ). Include confidence rules: 8-10 = 3 chiều đồng thuận, 5-7 = 2/3 đồng thuận, 1-4 = mâu thuẫn.

3. **ANALYSIS_TEMPERATURES** dict (per D-09-06):
```python
ANALYSIS_TEMPERATURES: dict[AnalysisType, float] = {
    AnalysisType.TECHNICAL: 0.1,
    AnalysisType.FUNDAMENTAL: 0.2,
    AnalysisType.SENTIMENT: 0.3,
    AnalysisType.COMBINED: 0.2,
}
```

4. **4 few-shot example constants** (per D-09-02). Each shows 1 example ticker with realistic VN stock data (use VNM for technical/fundamental, HPG for sentiment, VNM for combined). Format: "Example analysis:" → sample input data → "Expected output:" → sample JSON output. Keep each to ~10-15 lines. Store as module-level string constants:
   - `TECHNICAL_FEW_SHOT`: VNM example with RSI rising, bullish MACD crossover → signal=buy, strength=7
   - `FUNDAMENTAL_FEW_SHOT`: VNM example with PE=15.2, ROE=0.25 → health=good, score=7
   - `SENTIMENT_FEW_SHOT`: HPG example with 2 positive Vietnamese titles → sentiment=positive, score=7
   - `COMBINED_FEW_SHOT`: VNM example with tech=buy/7, fund=good/8, sent=positive/7 → recommendation=mua, confidence=8

5. **Refactor `_call_gemini_with_retry`** signature: add `temperature: float = 0.2` and `system_instruction: str | None = None` parameters. In the GenerateContentConfig, replace hardcoded `temperature=0.2` with the `temperature` param and add `system_instruction=system_instruction`. The `@retry` decorator is transparent to additional params (verified in RESEARCH.md Pitfall 5).

6. **Refactor `_call_gemini`** signature: add same `temperature` and `system_instruction` params. Pass them through to `_call_gemini_with_retry`:
```python
async def _call_gemini(self, prompt: str, response_schema, temperature: float = 0.2, system_instruction: str | None = None):
    return await gemini_breaker.call(self._call_gemini_with_retry, prompt, response_schema, temperature, system_instruction)
```
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_ai_analysis_service.py::TestAIAnalysisServiceInit -x -q</automated>
  </verify>
  <done>Module-level constants defined (SCORING_RUBRIC, 4 system instructions, 4 few-shot examples, ANALYSIS_TEMPERATURES dict). _call_gemini_with_retry and _call_gemini accept temperature + system_instruction params. GenerateContentConfig uses dynamic temperature and system_instruction. Existing init test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor prompt builders and wire batch analyzers</name>
  <files>backend/app/services/ai_analysis_service.py</files>
  <action>
**Refactor all 4 `_build_*_prompt` methods** (per D-09-01 — persona removed from user prompt; per D-09-02 — few-shot added; per D-09-05 — language enforced via system_instruction):

Each prompt builder should follow this new structure:
```
[few-shot example constant]

Now analyze the following tickers:

--- SYMBOL ---
[data lines unchanged]
```

1. **`_build_technical_prompt`**: Remove the persona paragraph ("You are a Vietnamese stock market...") and output format instructions. Keep the "Tickers:" data section exactly as-is (RSI, MACD, SMA, EMA, BB lines). Prepend the TECHNICAL_FEW_SHOT constant, then "Now analyze the following tickers based on their technical indicators from the last 5 trading days:" before the data.

2. **`_build_fundamental_prompt`**: Same pattern — remove persona and output format. Prepend FUNDAMENTAL_FEW_SHOT, then "Now analyze the following tickers based on their most recent financial data:" before the data.

3. **`_build_sentiment_prompt`**: Same pattern — remove persona and output format. Prepend SENTIMENT_FEW_SHOT, then "Phân tích các mã cổ phiếu sau:" (Vietnamese per D-09-05). Data section unchanged (keep "Không có tin tức gần đây." for empty news).

4. **`_build_combined_prompt`**: Same pattern — remove persona, output format, and confidence rules (now in system instruction). Prepend COMBINED_FEW_SHOT, then "Đưa ra khuyến nghị tổng hợp cho các mã sau:" before the data.

**Wire all 4 `_analyze_*_batch` methods** to pass the correct system instruction and temperature:

For each of `_analyze_technical_batch`, `_analyze_fundamental_batch`, `_analyze_sentiment_batch`, `_analyze_combined_batch`:
- Look up temperature: `temp = ANALYSIS_TEMPERATURES[AnalysisType.TECHNICAL]` (etc.)
- Look up system instruction: `sys_instr = TECHNICAL_SYSTEM_INSTRUCTION` (etc.)
- Pass both to `_call_gemini`: `await self._call_gemini(prompt, ResponseSchema, temp, sys_instr)`
- Do NOT change the fallback logic yet (Plan 02 handles low-temp retry)

**Update existing tests** so they pass after refactoring:
- `test_combined_prompt_includes_all_dimensions`: The assertion `assert "mua" in prompt.lower()` will fail because "mua" is now in system_instruction, not user prompt. Update to check that the data ("buy", "good", "positive", "VNM") is in the prompt, and that the few-shot example text is in the prompt. Alternatively, check for "khuyến nghị" or similar marker from the few-shot.
- `test_sentiment_prompt_includes_ticker_and_titles`: Check assertions still hold — "VNM", "FPT", "Vinamilk", "Không có tin tức" should all still be in the prompt data section. Only persona is removed.
- `test_sentiment_prompt_handles_empty_news`: "0 tin tức" still in data section → should pass.
- Other tests check for ticker symbols in prompts → will pass as data section is unchanged.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_ai_analysis_service.py -x -q</automated>
  </verify>
  <done>All 4 prompt builders produce user prompts with few-shot examples + data only (no persona). All 4 _analyze_*_batch methods pass type-specific temperature and system_instruction to _call_gemini. All existing tests pass (with minor assertion updates where needed). Prompt builders no longer contain "You are a" or "Bạn là" persona text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB data → prompt text | News titles from CafeF (already sanitized at crawl time) flow into user prompt |
| Gemini API response → application | Structured output parsed via Pydantic schema validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | News titles in sentiment prompt | mitigate | system_instruction separation (D-09-01) provides defense-in-depth — persona/rules isolated from user content where injected data lives |
| T-09-02 | Information Disclosure | API key in logs | accept | Already handled — API key passed via env var, never logged (no change in this phase) |
| T-09-03 | Denial of Service | Few-shot examples inflating prompt token count | accept | Examples are 1 per type (~100-200 tokens each) vs 25-ticker batches (~3000+ tokens) — marginal increase; token count already logged per batch |
</threat_model>

<verification>
1. `cd backend && python -m pytest tests/test_ai_analysis_service.py -x -q` — all existing tests pass
2. Grep confirms persona removed from prompt builders: `grep -n "You are a\|Bạn là" backend/app/services/ai_analysis_service.py` should match ONLY in system instruction constants, NOT in `_build_*_prompt` methods
3. Grep confirms system_instruction constants exist: `grep -n "SYSTEM_INSTRUCTION" backend/app/services/ai_analysis_service.py` shows 4 constants + usage in _analyze_*_batch
4. Grep confirms temperature dict: `grep -n "ANALYSIS_TEMPERATURES" backend/app/services/ai_analysis_service.py`
5. Grep confirms few-shot constants: `grep -n "FEW_SHOT" backend/app/services/ai_analysis_service.py` shows 4 constants + usage in _build_*_prompt
</verification>

<success_criteria>
- system_instruction parameter passed to GenerateContentConfig on every Gemini call
- Persona text exists only in *_SYSTEM_INSTRUCTION constants, not in _build_*_prompt methods
- SCORING_RUBRIC text present in all 4 system instruction constants
- ANALYSIS_TEMPERATURES dict maps all 4 AnalysisType values to correct temperatures
- 4 few-shot example constants defined and included in respective prompt builders
- English system instructions for TECHNICAL + FUNDAMENTAL; Vietnamese for SENTIMENT + COMBINED
- All existing tests pass after refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-prompt-improvements/09-01-SUMMARY.md`
</output>
