---
phase: 37-backend-analytics-consolidation
plan: 37-01
title: "Extract TradeBaseResponse, AnalyticsBase, refactor composition pattern"
wave: 1
tags: [BCK-01, BCK-02, BCK-03]
requirements_covered: [BCK-01, BCK-02, BCK-03]
goal: Duplicated analytics logic consolidated into shared abstractions with clean separation of concerns
---

## Tasks

### Task 1: TradeBaseResponse schema (BCK-03)

Create `backend/app/schemas/trade.py` with shared base:
- TradeBaseResponse: 21 shared fields
- BacktestTradeResponse(TradeBaseResponse): adds run_id, backtest_analysis_id
- PaperTradeResponse(TradeBaseResponse): no extra fields (or alias)

Update:
- `schemas/backtest.py`: import from trade.py
- `schemas/paper_trading.py`: import from trade.py
- `api/backtest.py` and `api/paper_trading.py`: verify imports still work

### Task 2: AnalyticsBase class (BCK-01)

Create `backend/app/services/analytics_base.py`:
- CLOSED_STATUSES constant
- `calc_win_rate(wins, total) → float`
- `calc_pnl_pct(pnl, capital) → float`
- `calc_avg_pnl(total_pnl, count) → float`
- `calc_max_drawdown(equity_points) → tuple[float, float]` (dd_vnd, dd_pct)

Refactor:
- `backtest_analytics_service.py`: inherit AnalyticsBase, use shared methods
- `paper_trade_analytics_service.py`: inherit AnalyticsBase, use shared methods

### Task 3: Composition pattern for BacktestAnalysisService (BCK-02)

Create `backend/app/services/analysis_context.py`:
- AnalysisContext protocol/ABC with methods: get_technical_context, get_combined_context, get_trading_signal_context, store_analysis
- LiveContext: current behavior from AIAnalysisService
- BacktestContext: date-aware queries from BacktestAnalysisService

Refactor:
- `ai_analysis_service.py`: accept optional AnalysisContext, default to LiveContext
- `backtest_analysis_service.py`: no longer inherits AIAnalysisService, instead passes BacktestContext

## Verification

1. `python -m pytest --tb=short -q` — all 689 tests pass
2. No duplicate CLOSED_STATUSES definitions
3. TradeBaseResponse used by both schemas
