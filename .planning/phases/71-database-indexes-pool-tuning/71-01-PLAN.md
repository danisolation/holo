---
phase: 71-database-indexes-pool-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/031_add_composite_indexes.py
  - backend/app/database.py
autonomous: true
requirements: [DB-IDX-01, DB-POOL-01]

must_haves:
  truths:
    - "Hot-path queries on technical_indicators, ai_analyses, daily_picks, weekly_reviews, job_executions have composite indexes"
    - "Connection pool is sized for concurrent API + scheduler load without exhaustion"
    - "Stale connections are recycled before Aiven closes them"
  artifacts:
    - path: "backend/alembic/versions/031_add_composite_indexes.py"
      provides: "Alembic migration creating 5 composite indexes"
      contains: "ix_technical_indicators_ticker_date"
    - path: "backend/app/database.py"
      provides: "Tuned pool settings"
      contains: "pool_recycle=1800"
  key_links:
    - from: "backend/alembic/versions/031_add_composite_indexes.py"
      to: "PostgreSQL catalog"
      via: "alembic upgrade head"
      pattern: "op\\.create_index"
---

<objective>
Add composite indexes on 5 hot tables (skipping daily_prices and rumors which already have them) and tune DB connection pool for concurrent load.

Purpose: Reduce query latency on hot paths (ticker lookups with date ordering) and prevent pool exhaustion under concurrent scheduler + API traffic.
Output: One Alembic migration + updated database.py pool config.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-database-indexes-pool-tuning/71-CONTEXT.md

@backend/app/database.py
@backend/app/models/technical_indicator.py
@backend/app/models/ai_analysis.py
@backend/app/models/daily_pick.py
@backend/app/models/weekly_review.py
@backend/app/models/job_execution.py

<interfaces>
<!-- Existing indexes (DO NOT duplicate): -->
<!-- daily_prices: ix_daily_prices_ticker_date(ticker_id, date DESC) — migration 027 -->
<!-- rumors: ix_rumors_ticker_posted(ticker_id, posted_at DESC) — in model __table_args__ -->

<!-- Current database.py pool config: -->
<!-- pool_size=5, max_overflow=3, pool_pre_ping=True -->

<!-- Alembic convention: sequential 3-digit revision IDs, down_revision chains -->
<!-- Latest revision: "030" (ai_accuracy table) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration for composite indexes + tune pool settings</name>
  <files>backend/alembic/versions/031_add_composite_indexes.py, backend/app/database.py</files>
  <action>
**Part A — Alembic migration (031_add_composite_indexes.py):**

Create migration file `backend/alembic/versions/031_add_composite_indexes.py` with:
- revision = "031", down_revision = "030"
- Docstring: "Add composite indexes on hot tables for query performance."

upgrade() creates these 5 indexes using `op.create_index()`:

1. `ix_technical_indicators_ticker_date` on `technical_indicators` — columns: `["ticker_id", sa.text("date DESC")]`
2. `ix_ai_analyses_ticker_type_date` on `ai_analyses` — columns: `["ticker_id", "analysis_type", sa.text("analysis_date DESC")]`
3. `ix_daily_picks_date_ticker` on `daily_picks` — columns: `[sa.text("pick_date DESC"), "ticker_id"]`
4. `ix_weekly_reviews_week_start` on `weekly_reviews` — columns: `[sa.text("week_start DESC")]`
5. `ix_job_executions_job_started` on `job_executions` — columns: `["job_id", sa.text("started_at DESC")]`

downgrade() drops all 5 indexes in reverse order.

SKIP daily_prices (already has ix_daily_prices_ticker_date from migration 027) and rumors (already has ix_rumors_ticker_posted in model definition).

Follow exact pattern from migration 027 for sa.text() DESC syntax.

**Part B — Pool tuning (database.py):**

Update `backend/app/database.py` engine creation:
- `pool_size=5` → `pool_size=10`
- `max_overflow=3` → `max_overflow=10`
- Add `pool_recycle=1800` (recycle connections every 30 min — Aiven may close idle connections)
- `pool_pre_ping=True` already exists — keep it
- Update the comment: `# Tuned for concurrent scheduler + API traffic (max 20 connections)`
  </action>
  <verify>
    <automated>cd backend && python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; c = Config('alembic.ini'); s = ScriptDirectory.from_config(c); head = s.get_current_head(); print(f'Head revision: {head}'); assert head == '031', f'Expected 031, got {head}'"</automated>
  </verify>
  <done>
    - Migration 031 exists with 5 CREATE INDEX statements (technical_indicators, ai_analyses, daily_picks, weekly_reviews, job_executions)
    - Migration 031 has matching DROP INDEX in downgrade
    - database.py has pool_size=10, max_overflow=10, pool_recycle=1800, pool_pre_ping=True
    - No duplicate indexes created for daily_prices or rumors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic → PostgreSQL | DDL statements execute with DB admin privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | D (Denial of Service) | Pool config | mitigate | pool_size=10 + max_overflow=10 = 20 max — within Aiven connection limit (~25). Monitor with pool status logging if issues arise. |
| T-71-02 | T (Tampering) | Migration | accept | Indexes are additive, non-destructive. Downgrade path exists. Low risk. |
</threat_model>

<verification>
1. Migration 031 file exists and follows project Alembic conventions
2. Alembic revision chain is unbroken (030 → 031)
3. database.py pool settings updated with all 4 parameters
4. No duplicate indexes for tables that already have them
</verification>

<success_criteria>
- Alembic head is revision 031
- 5 new composite indexes defined in migration
- Pool config: pool_size=10, max_overflow=10, pool_recycle=1800, pool_pre_ping=True
- When deployed: `alembic upgrade head` creates indexes, queries on hot tables use them (verified via EXPLAIN ANALYZE)
</success_criteria>

<output>
After completion, create `.planning/phases/71-database-indexes-pool-tuning/71-01-SUMMARY.md`
</output>
