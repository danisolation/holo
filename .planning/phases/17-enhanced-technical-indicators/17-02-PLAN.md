---
phase: 17-enhanced-technical-indicators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/accordion.tsx
  - frontend/src/lib/api.ts
  - frontend/src/components/indicator-chart.tsx
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03]

must_haves:
  truths:
    - "ATR (14) sub-chart renders as amber line chart in collapsible accordion item"
    - "ADX (14) sub-chart renders cyan ADX + green +DI + red -DI + dashed ref at 25"
    - "Stochastic (14, 3) sub-chart renders pink %K + indigo %D + dashed 80/20 ref lines"
    - "RSI and MACD charts are wrapped in accordion and expanded by default"
    - "ATR, ADX, Stochastic accordion items are collapsed by default"
    - "IndicatorData type has 6 new nullable number fields matching API contract"
  artifacts:
    - path: "frontend/src/components/ui/accordion.tsx"
      provides: "shadcn Accordion component"
      contains: "AccordionTrigger"
    - path: "frontend/src/lib/api.ts"
      provides: "IndicatorData with 18 fields"
      contains: "stoch_d_14"
    - path: "frontend/src/components/indicator-chart.tsx"
      provides: "ATRChart, ADXChart, StochasticChart + Accordion wrapper"
      contains: "ATRChart"
  key_links:
    - from: "frontend/src/components/indicator-chart.tsx"
      to: "frontend/src/components/ui/accordion.tsx"
      via: "import Accordion components"
      pattern: "import.*Accordion.*from.*ui/accordion"
    - from: "frontend/src/components/indicator-chart.tsx"
      to: "frontend/src/lib/api.ts"
      via: "import IndicatorData type"
      pattern: "import.*IndicatorData.*from.*lib/api"
    - from: "frontend/src/components/indicator-chart.tsx"
      to: "lightweight-charts"
      via: "createChart + LineSeries for new charts"
      pattern: "createChart\\(container"
---

<objective>
Add ATR, ADX, and Stochastic sub-chart components to the indicator display, wrap all 5 indicator charts (RSI, MACD, ATR, ADX, Stochastic) in a shadcn Accordion, and extend the IndicatorData TypeScript type with 6 new fields.

Purpose: Users can view volatility (ATR), trend strength (ADX), and momentum (Stochastic) charts on the ticker detail page, with collapsible accordion to manage visual density.
Output: Three new chart components + Accordion wrapper + updated TypeScript types.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-enhanced-technical-indicators/17-CONTEXT.md
@.planning/phases/17-enhanced-technical-indicators/17-RESEARCH.md
@.planning/phases/17-enhanced-technical-indicators/17-UI-SPEC.md

@frontend/src/components/indicator-chart.tsx
@frontend/src/lib/api.ts
@frontend/components.json
@frontend/src/app/ticker/[symbol]/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/lib/api.ts:
```typescript
export interface IndicatorData {
  ticker_symbol: string;
  date: string;
  rsi_14: number | null;
  macd_line: number | null;
  macd_signal: number | null;
  macd_histogram: number | null;
  sma_20: number | null;
  sma_50: number | null;
  sma_200: number | null;
  ema_12: number | null;
  ema_26: number | null;
  bb_upper: number | null;
  bb_middle: number | null;
  bb_lower: number | null;
  // Must add 6 new fields:
  // atr_14, adx_14, plus_di_14, minus_di_14, stoch_k_14, stoch_d_14
}
```

From frontend/src/components/indicator-chart.tsx:
```typescript
// Current structure — RSIChart and MACDChart are internal functions
// IndicatorChart is the only export, renders RSI + MACD in a div.space-y-4
// Must: add ATRChart, ADXChart, StochasticChart, replace div wrapper with Accordion

interface IndicatorChartProps {
  indicatorData: IndicatorData[];
}

function RSIChart({ indicatorData }: { indicatorData: IndicatorData[] }) { ... }
function MACDChart({ indicatorData }: { indicatorData: IndicatorData[] }) { ... }
export function IndicatorChart({ indicatorData }: IndicatorChartProps) { ... }
```

From frontend/components.json:
```json
{
  "style": "base-nova",
  "rsc": true,
  "tsx": true,
  "iconLibrary": "lucide",
  "aliases": {
    "ui": "@/components/ui"
  }
}
// shadcn install: npx shadcn@latest add accordion
```

Chart configuration pattern (from existing RSIChart):
```typescript
const chart = createChart(container, {
  layout: { background: { color: "#0f172a" }, textColor: "#cbd5e1" },
  grid: { vertLines: { color: "#1e293b" }, horzLines: { color: "#1e293b" } },
  width: container.clientWidth,
  height: 160,
  rightPriceScale: { borderColor: "#334155", scaleMargins: { top: 0.1, bottom: 0.1 } },
  timeScale: { borderColor: "#334155", timeVisible: false },
  crosshair: { mode: 0 },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Accordion + extend IndicatorData type</name>
  <files>
    frontend/src/components/ui/accordion.tsx
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/components.json
    - .planning/phases/17-enhanced-technical-indicators/17-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Install shadcn Accordion component**

Run from `frontend/` directory:
```
npx shadcn@latest add accordion --yes
```

This generates `frontend/src/components/ui/accordion.tsx` using the base-nova style with @base-ui/react (already installed as ^1.4.0). The component provides `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent` exports.

If the `npx` command fails or prompts interactively, create the accordion component manually following shadcn base-nova patterns with @base-ui/react Collapsible.

**Step 2: Extend IndicatorData interface in `frontend/src/lib/api.ts`**

Add 6 new nullable fields to the `IndicatorData` interface after `bb_lower`:

```typescript
export interface IndicatorData {
  ticker_symbol: string;
  date: string;
  rsi_14: number | null;
  macd_line: number | null;
  macd_signal: number | null;
  macd_histogram: number | null;
  sma_20: number | null;
  sma_50: number | null;
  sma_200: number | null;
  ema_12: number | null;
  ema_26: number | null;
  bb_upper: number | null;
  bb_middle: number | null;
  bb_lower: number | null;
  // Phase 17: Volatility, Trend Strength, Momentum
  atr_14: number | null;
  adx_14: number | null;
  plus_di_14: number | null;
  minus_di_14: number | null;
  stoch_k_14: number | null;
  stoch_d_14: number | null;
}
```

The field names exactly match the backend API response keys from `IndicatorResponse` schema.
  </action>
  <verify>
    <automated>Test-Path "frontend/src/components/ui/accordion.tsx"; Select-String -Path "frontend/src/lib/api.ts" -Pattern "atr_14|adx_14|stoch_k_14|stoch_d_14" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/ui/accordion.tsx` exists
    - `grep -c "AccordionTrigger" frontend/src/components/ui/accordion.tsx` returns at least 1
    - `grep -c "AccordionContent" frontend/src/components/ui/accordion.tsx` returns at least 1
    - `grep -c "atr_14: number | null" frontend/src/lib/api.ts` returns 1
    - `grep -c "adx_14: number | null" frontend/src/lib/api.ts` returns 1
    - `grep -c "plus_di_14: number | null" frontend/src/lib/api.ts` returns 1
    - `grep -c "minus_di_14: number | null" frontend/src/lib/api.ts` returns 1
    - `grep -c "stoch_k_14: number | null" frontend/src/lib/api.ts` returns 1
    - `grep -c "stoch_d_14: number | null" frontend/src/lib/api.ts` returns 1
  </acceptance_criteria>
  <done>shadcn Accordion component installed at ui/accordion.tsx. IndicatorData interface extended with 6 new nullable number fields matching backend API contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add ATRChart, ADXChart, StochasticChart + Accordion wrapper</name>
  <files>frontend/src/components/indicator-chart.tsx</files>
  <read_first>
    - frontend/src/components/indicator-chart.tsx
    - frontend/src/components/ui/accordion.tsx
    - frontend/src/lib/api.ts
    - .planning/phases/17-enhanced-technical-indicators/17-UI-SPEC.md
    - .planning/phases/17-enhanced-technical-indicators/17-RESEARCH.md
  </read_first>
  <action>
Rewrite `frontend/src/components/indicator-chart.tsx` to add 3 new chart components and wrap all 5 in an Accordion.

**Step 1: Add Accordion import** at the top of the file:
```typescript
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";
```

**Step 2: Keep existing RSIChart and MACDChart functions unchanged** (lines 19-225). Do NOT modify their internal logic. Only their `<h4>` labels will move to AccordionTrigger when wrapped.

**Step 3: Add ATRChart function** after MACDChart:

```typescript
/**
 * ATR sub-chart: ATR(14) line showing price volatility.
 */
function ATRChart({ indicatorData }: { indicatorData: IndicatorData[] }) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const container = containerRef.current;
    if (!container) return;

    const atrData = indicatorData
      .filter((d) => d.atr_14 != null)
      .map((d) => ({ time: d.date as string, value: d.atr_14! }))
      .sort((a, b) => (a.time as string).localeCompare(b.time as string));

    if (atrData.length === 0) return;

    const chart = createChart(container, {
      layout: { background: { color: "#0f172a" }, textColor: "#cbd5e1" },
      grid: { vertLines: { color: "#1e293b" }, horzLines: { color: "#1e293b" } },
      width: container.clientWidth,
      height: 160,
      rightPriceScale: {
        borderColor: "#334155",
        scaleMargins: { top: 0.1, bottom: 0.1 },
      },
      timeScale: { borderColor: "#334155", timeVisible: false },
      crosshair: { mode: 0 },
    });

    const atrSeries = chart.addSeries(LineSeries, {
      color: "#FBBF24",
      lineWidth: 2,
      priceLineVisible: false,
      lastValueVisible: true,
    });
    atrSeries.setData(atrData);

    chart.timeScale().fitContent();

    const observer = new ResizeObserver((entries) => {
      for (const entry of entries) {
        chart.applyOptions({ width: entry.contentRect.width });
      }
    });
    observer.observe(container);

    return () => { observer.disconnect(); chart.remove(); };
  }, [indicatorData]);

  return (
    <div>
      <h4 className="text-xs font-medium text-muted-foreground mb-1 px-1">
        ATR (14)
        <span className="ml-2 text-[10px]">
          <span className="text-[#FBBF24]">Biến động giá</span>
        </span>
      </h4>
      <div ref={containerRef} className="rounded-lg overflow-hidden" />
    </div>
  );
}
```

**Step 4: Add ADXChart function** after ATRChart:

```typescript
/**
 * ADX sub-chart: ADX line + +DI + -DI with reference line at 25.
 */
function ADXChart({ indicatorData }: { indicatorData: IndicatorData[] }) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const container = containerRef.current;
    if (!container) return;

    const adxData = indicatorData
      .filter((d) => d.adx_14 != null && d.plus_di_14 != null && d.minus_di_14 != null)
      .sort((a, b) => a.date.localeCompare(b.date));

    if (adxData.length === 0) return;

    const chart = createChart(container, {
      layout: { background: { color: "#0f172a" }, textColor: "#cbd5e1" },
      grid: { vertLines: { color: "#1e293b" }, horzLines: { color: "#1e293b" } },
      width: container.clientWidth,
      height: 160,
      rightPriceScale: {
        borderColor: "#334155",
        scaleMargins: { top: 0.1, bottom: 0.1 },
      },
      timeScale: { borderColor: "#334155", timeVisible: false },
      crosshair: { mode: 0 },
    });

    // ADX line (cyan, primary)
    const adxLine = chart.addSeries(LineSeries, {
      color: "#06B6D4",
      lineWidth: 2,
      priceLineVisible: false,
      lastValueVisible: true,
    });
    adxLine.setData(
      adxData.map((d) => ({ time: d.date as string, value: d.adx_14! }))
    );

    // +DI line (green)
    const posLine = chart.addSeries(LineSeries, {
      color: "#22C55E",
      lineWidth: 1,
      priceLineVisible: false,
      lastValueVisible: false,
    });
    posLine.setData(
      adxData.map((d) => ({ time: d.date as string, value: d.plus_di_14! }))
    );

    // -DI line (red)
    const negLine = chart.addSeries(LineSeries, {
      color: "#EF4444",
      lineWidth: 1,
      priceLineVisible: false,
      lastValueVisible: false,
    });
    negLine.setData(
      adxData.map((d) => ({ time: d.date as string, value: d.minus_di_14! }))
    );

    // Reference line at 25 (strong trend threshold)
    const refLine = chart.addSeries(LineSeries, {
      color: "rgba(255,255,255,0.25)",
      lineWidth: 1,
      lineStyle: 2,
      priceLineVisible: false,
      lastValueVisible: false,
    });
    refLine.setData(
      adxData.map((d) => ({ time: d.date as string, value: 25 }))
    );

    chart.timeScale().fitContent();

    const observer = new ResizeObserver((entries) => {
      for (const entry of entries) {
        chart.applyOptions({ width: entry.contentRect.width });
      }
    });
    observer.observe(container);

    return () => { observer.disconnect(); chart.remove(); };
  }, [indicatorData]);

  return (
    <div>
      <h4 className="text-xs font-medium text-muted-foreground mb-1 px-1">
        ADX (14)
        <span className="ml-2 text-[10px]">
          <span className="text-[#06B6D4]">ADX</span>
          {" · "}
          <span className="text-[#22C55E]">+DI</span>
          {" · "}
          <span className="text-[#EF4444]">-DI</span>
          {" · "}
          <span className="text-white/25">25 xu hướng mạnh</span>
        </span>
      </h4>
      <div ref={containerRef} className="rounded-lg overflow-hidden" />
    </div>
  );
}
```

**Step 5: Add StochasticChart function** after ADXChart:

```typescript
/**
 * Stochastic sub-chart: %K and %D lines with 80/20 overbought/oversold zones.
 */
function StochasticChart({ indicatorData }: { indicatorData: IndicatorData[] }) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const container = containerRef.current;
    if (!container) return;

    const stochData = indicatorData
      .filter((d) => d.stoch_k_14 != null && d.stoch_d_14 != null)
      .sort((a, b) => a.date.localeCompare(b.date));

    if (stochData.length === 0) return;

    const chart = createChart(container, {
      layout: { background: { color: "#0f172a" }, textColor: "#cbd5e1" },
      grid: { vertLines: { color: "#1e293b" }, horzLines: { color: "#1e293b" } },
      width: container.clientWidth,
      height: 160,
      rightPriceScale: {
        borderColor: "#334155",
        scaleMargins: { top: 0.1, bottom: 0.1 },
      },
      timeScale: { borderColor: "#334155", timeVisible: false },
      crosshair: { mode: 0 },
    });

    // %K line (pink, primary)
    const kLine = chart.addSeries(LineSeries, {
      color: "#EC4899",
      lineWidth: 2,
      priceLineVisible: false,
      lastValueVisible: true,
    });
    kLine.setData(
      stochData.map((d) => ({ time: d.date as string, value: d.stoch_k_14! }))
    );

    // %D line (indigo, signal)
    const dLine = chart.addSeries(LineSeries, {
      color: "#818CF8",
      lineWidth: 1,
      priceLineVisible: false,
      lastValueVisible: false,
    });
    dLine.setData(
      stochData.map((d) => ({ time: d.date as string, value: d.stoch_d_14! }))
    );

    // Overbought line (80)
    const overbought = chart.addSeries(LineSeries, {
      color: "rgba(239,83,80,0.4)",
      lineWidth: 1,
      lineStyle: 2,
      priceLineVisible: false,
      lastValueVisible: false,
    });
    overbought.setData(
      stochData.map((d) => ({ time: d.date as string, value: 80 }))
    );

    // Oversold line (20)
    const oversold = chart.addSeries(LineSeries, {
      color: "rgba(38,166,154,0.4)",
      lineWidth: 1,
      lineStyle: 2,
      priceLineVisible: false,
      lastValueVisible: false,
    });
    oversold.setData(
      stochData.map((d) => ({ time: d.date as string, value: 20 }))
    );

    chart.timeScale().fitContent();

    const observer = new ResizeObserver((entries) => {
      for (const entry of entries) {
        chart.applyOptions({ width: entry.contentRect.width });
      }
    });
    observer.observe(container);

    return () => { observer.disconnect(); chart.remove(); };
  }, [indicatorData]);

  return (
    <div>
      <h4 className="text-xs font-medium text-muted-foreground mb-1 px-1">
        Stochastic (14, 3)
        <span className="ml-2 text-[10px]">
          <span className="text-[#EC4899]">%K</span>
          {" · "}
          <span className="text-[#818CF8]">%D</span>
          {" · "}
          <span className="text-[#ef5350]/40">80 quá mua</span>
          {" · "}
          <span className="text-[#26a69a]/40">20 quá bán</span>
        </span>
      </h4>
      <div ref={containerRef} className="rounded-lg overflow-hidden" />
    </div>
  );
}
```

**Step 6: Rewrite the exported IndicatorChart function** to wrap all 5 charts in Accordion:

Replace the current `IndicatorChart` function (the only export) with:

```typescript
/**
 * Combined indicator charts: all 5 sub-panes in collapsible accordion.
 * RSI and MACD expanded by default (existing behavior).
 * ATR, ADX, Stochastic collapsed by default (new, opt-in).
 */
export function IndicatorChart({ indicatorData }: IndicatorChartProps) {
  if (indicatorData.length === 0) {
    return (
      <div className="flex items-center justify-center h-40 text-muted-foreground text-sm">
        Không có dữ liệu chỉ báo kỹ thuật
      </div>
    );
  }

  return (
    <Accordion type="multiple" defaultValue={["rsi", "macd"]}>
      <AccordionItem value="rsi">
        <AccordionTrigger className="text-xs font-medium text-muted-foreground px-1 py-2">
          RSI (14)
          <span className="ml-2 text-[10px]">
            <span className="text-[#ef5350]">70 quá mua</span>
            {" · "}
            <span className="text-[#26a69a]">30 quá bán</span>
          </span>
        </AccordionTrigger>
        <AccordionContent>
          <RSIChart indicatorData={indicatorData} />
        </AccordionContent>
      </AccordionItem>

      <AccordionItem value="macd">
        <AccordionTrigger className="text-xs font-medium text-muted-foreground px-1 py-2">
          MACD (12, 26, 9)
          <span className="ml-2 text-[10px]">
            <span className="text-[#2196F3]">MACD</span>
            {" · "}
            <span className="text-[#FF9800]">Signal</span>
          </span>
        </AccordionTrigger>
        <AccordionContent>
          <MACDChart indicatorData={indicatorData} />
        </AccordionContent>
      </AccordionItem>

      <AccordionItem value="atr">
        <AccordionTrigger className="text-xs font-medium text-muted-foreground px-1 py-2">
          ATR (14)
          <span className="ml-2 text-[10px]">
            <span className="text-[#FBBF24]">Biến động giá</span>
          </span>
        </AccordionTrigger>
        <AccordionContent>
          <ATRChart indicatorData={indicatorData} />
        </AccordionContent>
      </AccordionItem>

      <AccordionItem value="adx">
        <AccordionTrigger className="text-xs font-medium text-muted-foreground px-1 py-2">
          ADX (14)
          <span className="ml-2 text-[10px]">
            <span className="text-[#06B6D4]">ADX</span>
            {" · "}
            <span className="text-[#22C55E]">+DI</span>
            {" · "}
            <span className="text-[#EF4444]">-DI</span>
          </span>
        </AccordionTrigger>
        <AccordionContent>
          <ADXChart indicatorData={indicatorData} />
        </AccordionContent>
      </AccordionItem>

      <AccordionItem value="stochastic">
        <AccordionTrigger className="text-xs font-medium text-muted-foreground px-1 py-2">
          Stochastic (14, 3)
          <span className="ml-2 text-[10px]">
            <span className="text-[#EC4899]">%K</span>
            {" · "}
            <span className="text-[#818CF8]">%D</span>
          </span>
        </AccordionTrigger>
        <AccordionContent>
          <StochasticChart indicatorData={indicatorData} />
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  );
}
```

**IMPORTANT NOTES:**
- Remove the `<h4>` labels from INSIDE existing RSIChart and MACDChart return JSX — the label info is now in AccordionTrigger. Keep the `<div ref={containerRef} className="rounded-lg overflow-hidden" />` container div.
- The new ATRChart, ADXChart, StochasticChart also do NOT need their internal `<h4>` labels since the label is in AccordionTrigger. Remove the `<h4>` from the returned JSX of all 5 chart components, keeping only the container div.
- Each chart function's internal logic (useEffect, createChart, data filtering, ResizeObserver) stays identical.
- `defaultValue={["rsi", "macd"]}` means RSI and MACD are expanded by default; ATR, ADX, Stochastic are collapsed.
- `type="multiple"` allows multiple charts to be open simultaneously.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | Select-String -Pattern "(error|warning|Compiled|Build)" | Select-Object -First 15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function ATRChart" frontend/src/components/indicator-chart.tsx` returns 1
    - `grep -c "function ADXChart" frontend/src/components/indicator-chart.tsx` returns 1
    - `grep -c "function StochasticChart" frontend/src/components/indicator-chart.tsx` returns 1
    - `grep -c "Accordion" frontend/src/components/indicator-chart.tsx` returns at least 3 (import + usage)
    - `grep -c "AccordionItem" frontend/src/components/indicator-chart.tsx` returns at least 5 (one per chart)
    - `grep -c 'defaultValue={\["rsi", "macd"\]}' frontend/src/components/indicator-chart.tsx` returns 1
    - `grep -c 'type="multiple"' frontend/src/components/indicator-chart.tsx` returns 1
    - `grep -c "#FBBF24" frontend/src/components/indicator-chart.tsx` returns at least 1 (ATR amber)
    - `grep -c "#06B6D4" frontend/src/components/indicator-chart.tsx` returns at least 1 (ADX cyan)
    - `grep -c "#EC4899" frontend/src/components/indicator-chart.tsx` returns at least 1 (Stoch %K pink)
    - `grep -c "#818CF8" frontend/src/components/indicator-chart.tsx` returns at least 1 (Stoch %D indigo)
    - `grep -c "#22C55E" frontend/src/components/indicator-chart.tsx` returns at least 1 (+DI green)
    - `grep -c "#EF4444" frontend/src/components/indicator-chart.tsx` returns at least 1 (-DI red)
    - `grep -c "rgba(255,255,255,0.25)" frontend/src/components/indicator-chart.tsx` returns at least 1 (ADX ref 25)
    - `grep -c "value: 25" frontend/src/components/indicator-chart.tsx` returns at least 1 (ADX ref at 25)
    - `grep -c "value: 80" frontend/src/components/indicator-chart.tsx` returns at least 1 (Stoch overbought)
    - `grep -c "value: 20" frontend/src/components/indicator-chart.tsx` returns at least 1 (Stoch oversold)
    - `grep -c "Biến động giá" frontend/src/components/indicator-chart.tsx` returns at least 1 (ATR Vietnamese label)
    - Next.js build compiles without errors
  </acceptance_criteria>
  <done>Three new chart components (ATRChart, ADXChart, StochasticChart) render with correct colors, reference lines, and legends. All 5 indicator charts wrapped in Accordion with RSI/MACD expanded and ATR/ADX/Stochastic collapsed by default. Next.js build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → chart rendering | Indicator data from API rendered in charts — no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | Chart data | accept | Same public financial data already displayed for RSI/MACD. No PII. No new API calls. |
| T-17-04 | Denial of Service | Chart rendering | accept | Same createChart pattern as existing RSI/MACD. Max data points bounded by API limit=500. |
</threat_model>

<verification>
1. `frontend/src/components/ui/accordion.tsx` exists (shadcn installed)
2. `frontend/src/lib/api.ts` IndicatorData has 18 fields (12 existing + 6 new)
3. `frontend/src/components/indicator-chart.tsx` has ATRChart, ADXChart, StochasticChart functions
4. Accordion wraps all 5 charts with `type="multiple"` and `defaultValue={["rsi", "macd"]}`
5. `cd frontend && npx next build` compiles without errors
</verification>

<success_criteria>
- shadcn Accordion installed and functional
- IndicatorData type extended with atr_14, adx_14, plus_di_14, minus_di_14, stoch_k_14, stoch_d_14
- ATR chart renders amber (#FBBF24) line
- ADX chart renders 3 lines (cyan ADX, green +DI, red -DI) + dashed reference at 25
- Stochastic chart renders 2 lines (pink %K, indigo %D) + dashed 80/20 reference lines
- All 5 charts in Accordion with RSI/MACD expanded, ATR/ADX/Stochastic collapsed by default
- Next.js build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-enhanced-technical-indicators/17-02-SUMMARY.md`
</output>
