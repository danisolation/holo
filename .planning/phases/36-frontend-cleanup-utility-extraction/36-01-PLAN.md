---
phase: 36-frontend-cleanup-utility-extraction
plan: 36-01
title: "Remove DilutionBadge, extract format utilities, consolidate config constants"
wave: 1
tags: [CLN-05, CLN-06, FRN-03]
requirements_covered: [CLN-05, CLN-06, FRN-03]
goal: Dead frontend components removed and duplicated utility functions/config objects consolidated into shared modules
---

## Context

Phase 36 cleans up the frontend: remove dead DilutionBadge component, extract 18+ duplicated format functions into src/lib/format.ts, and consolidate STATUS_CONFIG/SIGNAL_CONFIG into src/lib/constants.ts.

## Tasks

### Task 1: Remove DilutionBadge (CLN-05)

**Actions:**
1. Delete `frontend/src/components/dilution-badge.tsx`
2. Edit `frontend/src/components/holdings-table.tsx` — remove import and usage of DilutionBadge

**Acceptance:** No file named dilution-badge exists; grep for "DilutionBadge" returns zero hits.

### Task 2: Create src/lib/format.ts and replace all duplicates (CLN-06)

**Actions:**
1. Create `frontend/src/lib/format.ts` exporting:
   - `formatVND(value: number): string` — Intl.NumberFormat vi-VN
   - `formatCompactVND(value: number): string` — compact notation
   - `formatDateVN(dateStr: string): string` — DD/MM/YYYY format

2. Replace local `formatVND` in 16 files:
   - allocation-chart.tsx
   - backtest/bt-analytics-tab.tsx
   - backtest/bt-config-tab.tsx
   - backtest/bt-results-tab.tsx
   - backtest/bt-trades-tab.tsx
   - csv-preview-table.tsx
   - holdings-table.tsx
   - paper-trading/pt-analytics-tab.tsx
   - paper-trading/pt-equity-chart.tsx
   - paper-trading/pt-overview-tab.tsx
   - paper-trading/pt-periodic-table.tsx
   - paper-trading/pt-timeframe-compare.tsx
   - paper-trading/pt-trades-table.tsx
   - performance-chart.tsx
   - portfolio-summary.tsx
   - trade-delete-confirm.tsx
   - trade-history.tsx

3. Replace local `formatCompactVND` in 4 files:
   - allocation-chart.tsx
   - backtest/bt-analytics-tab.tsx
   - performance-chart.tsx
   - paper-trading/pt-equity-chart.tsx

4. Replace local `formatDateVN` in 1 file (dilution-badge already deleted):
   - corporate-events-calendar.tsx

**Acceptance:** grep for "function formatVND" returns only format.ts; all components import from @/lib/format.

### Task 3: Create src/lib/constants.ts and consolidate configs (FRN-03)

**Actions:**
1. Create `frontend/src/lib/constants.ts` exporting:
   - `TRADE_STATUS_CONFIG` — merged from bt-trades-tab and pt-trades-table (superset with closed_manual)
   - `SIGNAL_CONFIG` + `getSignalConfig()` — moved from analysis-card.tsx
   - Note: connection-status STATUS_CONFIG is a DIFFERENT type (ConnectionStatus) — leave it local

2. Update bt-trades-tab.tsx — import TRADE_STATUS_CONFIG from @/lib/constants
3. Update pt-trades-table.tsx — import TRADE_STATUS_CONFIG from @/lib/constants
4. Update analysis-card.tsx — import SIGNAL_CONFIG and getSignalConfig from @/lib/constants

**Acceptance:** grep for "const STATUS_CONFIG" in bt-trades-tab and pt-trades-table returns zero; SIGNAL_CONFIG defined only in constants.ts.

## Verification

1. `npx tsc --noEmit` passes (TypeScript compiles)
2. No grep hits for "function formatVND" outside format.ts
3. No grep hits for "DilutionBadge" anywhere
4. All imports resolve correctly
