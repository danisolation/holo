---
phase: 99-performance-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/watchlist/page.tsx
  - frontend/src/app/discovery/page.tsx
  - frontend/src/app/simulator/page.tsx
  - frontend/src/app/ticker/[symbol]/page.tsx
  - frontend/src/components/watchlist-table.tsx
  - frontend/src/components/discovery-table.tsx
  - frontend/src/components/simulator/portfolio-summary.tsx
  - frontend/src/components/simulator/positions-table.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "All data-fetching pages show skeleton placeholders during loading (not blank/spinner text)"
    - "Simulator page renders correctly on mobile viewport (≤768px) without horizontal overflow"
    - "Discovery table scrolls horizontally on mobile without page overflow"
    - "Watchlist table is usable on mobile with horizontal scroll"
    - "Ticker detail page doesn't overflow on mobile"
  artifacts:
    - path: "frontend/src/app/watchlist/page.tsx"
      provides: "Skeleton loading state for watchlist page"
    - path: "frontend/src/app/discovery/page.tsx"
      provides: "Skeleton loading state for discovery page"
    - path: "frontend/src/app/simulator/page.tsx"
      provides: "Skeleton loading state for simulator"
    - path: "frontend/src/app/ticker/[symbol]/page.tsx"
      provides: "Skeleton loading state for ticker detail"
  key_links:
    - from: "all page components"
      to: "@/components/ui/skeleton"
      via: "isLoading conditional rendering"
      pattern: "isLoading.*Skeleton"
---

<objective>
Add skeleton loading states to all data-fetching pages and polish mobile responsiveness across the app.

Purpose: Eliminate blank/spinner "Đang tải..." text during data fetch; ensure all pages render without overflow on mobile viewports.
Output: All pages show skeleton placeholders during loading; tables scroll horizontally on mobile; simulator/discovery layouts adapt to narrow viewports.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/ui/skeleton.tsx
@frontend/src/app/page.tsx (reference — already has skeleton pattern)
@frontend/src/app/watchlist/page.tsx
@frontend/src/app/discovery/page.tsx
@frontend/src/app/simulator/page.tsx
@frontend/src/lib/hooks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading states to all data-fetching pages</name>
  <files>frontend/src/app/watchlist/page.tsx, frontend/src/app/discovery/page.tsx, frontend/src/app/simulator/page.tsx, frontend/src/app/ticker/[symbol]/page.tsx</files>
  <action>
Add skeleton loading states to replace text-based "Đang tải..." messages:

1. **watchlist/page.tsx**: Use `isLoading` from `useWatchlist()` hook. When loading, show skeleton grid: a header skeleton (h-8 w-48) + 8 rows of table-row skeletons (h-12 each, 5 columns with varying widths). Use the existing `Skeleton` component from `@/components/ui/skeleton`.

2. **discovery/page.tsx**: Destructure `{ data, isLoading }` from `useDiscovery()`. When `isLoading`, show a skeleton mimicking the discovery table: header skeleton + 10 rows of `Skeleton` (h-10 each, in a card container). Only show empty state when `!isLoading && data?.length === 0`.

3. **simulator/page.tsx**: Replace the text `"Đang tải danh mục..."` paragraph (line 90-92) with a proper skeleton: 4 stat cards (h-24 each in grid-cols-2 md:grid-cols-4 gap-4) + a table skeleton (6 rows h-10). Keep the same conditional structure (`isLoading ? skeleton : portfolio ? content : null`).

4. **ticker/[symbol]/page.tsx**: Add skeleton loading state for the ticker detail page. Show: title skeleton (h-8 w-32) + price skeleton (h-12 w-48) + chart placeholder (h-[400px] w-full) + 3 card skeletons (h-40 each).

All skeleton UIs must use `Skeleton` from `@/components/ui/skeleton`. All text remains Vietnamese.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | Select-String -Pattern "(error|Error|failed)" | Select-Object -First 10</automated>
  </verify>
  <done>All 4 pages show skeleton loading states during data fetch; no text-based loading messages remain; build passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile responsive polish for tables and simulator layout</name>
  <files>frontend/src/components/watchlist-table.tsx, frontend/src/components/discovery-table.tsx, frontend/src/components/simulator/positions-table.tsx, frontend/src/app/simulator/page.tsx</files>
  <action>
Add mobile responsive improvements to prevent horizontal overflow:

1. **watchlist-table.tsx**: Wrap the `<Table>` in a `<div className="overflow-x-auto -mx-4 px-4 md:mx-0 md:px-0">` container so it scrolls horizontally on mobile. Add `min-w-[700px]` to the Table itself to prevent column crushing.

2. **discovery-table.tsx**: Same pattern — wrap Table in overflow-x-auto container. Add `min-w-[600px]` to the Table.

3. **simulator/positions-table.tsx**: Wrap in overflow-x-auto container with `min-w-[650px]` on the Table. Ensure position cards don't overflow.

4. **simulator/page.tsx**: Make the TabsList scrollable on mobile by wrapping it or adding `overflow-x-auto` to its container. The tabs list has 5 items which can overflow on narrow screens. Add `className="w-full overflow-x-auto"` wrapper around TabsList, and add `flex-shrink-0` to each TabsTrigger if needed.

5. **General mobile check**: Ensure the controls row (AutoTradeToggle + Reset button) on simulator page wraps correctly on mobile by changing `flex items-center justify-between` to include `flex-wrap gap-2`.

Do NOT change any functionality — only add responsive CSS utilities. Do NOT modify the navbar (it already handles mobile via Sheet).
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | Select-String -Pattern "(error|Error|failed)" | Select-Object -First 10</automated>
  </verify>
  <done>Tables scroll horizontally on mobile without page overflow; simulator tabs don't overflow on narrow viewport; build passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure frontend CSS/UI changes — no new data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | I (Information Disclosure) | skeleton states | accept | Skeletons show no real data; loading states are non-sensitive |
</threat_model>

<verification>
- `cd frontend && npx next build` passes with no errors
- All pages show skeleton loading (verifiable by throttling network in dev tools)
- No horizontal page-level overflow on mobile viewport (375px width)
</verification>

<success_criteria>
- All 4 data-fetching pages (watchlist, discovery, simulator, ticker) render skeleton placeholders during loading
- Tables horizontally scroll on mobile without breaking page layout
- Simulator tabs accessible on mobile without overflow
- Frontend build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-performance-ux-polish/99-01-SUMMARY.md`
</output>
