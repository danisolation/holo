---
phase: 05-dashboard
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/app/watchlist/page.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/navbar.tsx
  - frontend/src/components/watchlist-table.tsx
  - frontend/src/app/layout.tsx
autonomous: true
requirements: [DASH-03, DASH-05]

must_haves:
  truths:
    - "Watchlist page shows table of watched tickers with latest price, change %, signal"
    - "User can remove tickers from watchlist on the watchlist page"
    - "Dashboard summary page shows portfolio-like overview of watchlist tickers"
    - "Navigation bar links to all 4 pages (home, watchlist, dashboard, search)"
    - "All pages render correctly on mobile (< 768px)"
    - "Dark mode is default and toggle works"
    - "Next.js app builds successfully with no TypeScript errors"
  artifacts:
    - path: "frontend/src/app/watchlist/page.tsx"
      provides: "Watchlist page with ticker table"
      contains: "WatchlistTable"
    - path: "frontend/src/components/navbar.tsx"
      provides: "Responsive navigation bar"
      contains: "Navbar"
    - path: "frontend/src/components/watchlist-table.tsx"
      provides: "Watchlist data table with @tanstack/react-table"
      contains: "useReactTable"
---

<objective>
Build the watchlist page, dashboard summary, navigation, and responsive polish.

Purpose: Completes DASH-03 (watchlist) and DASH-05 (responsive). Final polish for all pages.
Output: Complete dashboard with navigation, watchlist management, and mobile-friendly layout.
</objective>

<tasks>
## Task 1: Navigation & Watchlist

### 1.1: Create responsive navbar
Create `frontend/src/components/navbar.tsx`:
- Logo "Holo" linking to /
- Navigation links: Overview, Watchlist, Dashboard
- Ticker search (compact, in nav)
- Dark mode toggle button
- Mobile: hamburger menu with slide-out drawer
- Use shadcn Sheet for mobile menu

### 1.2: Create watchlist table component
Create `frontend/src/components/watchlist-table.tsx`:
- Uses @tanstack/react-table
- Columns: Symbol, Name, Last Price, Change %, Signal, Actions (remove)
- Sortable by any column
- Data from zustand watchlist + React Query for each ticker's data
- Empty state: "No tickers in watchlist. Browse the market overview to add some."

### 1.3: Build watchlist page
Create `frontend/src/app/watchlist/page.tsx`:
- Title: "Danh mục theo dõi" (Watchlist)
- WatchlistTable component
- Quick add: search bar to add tickers directly
- Link to ticker detail on row click

## Task 2: Dashboard Summary & Polish

### 2.1: Build dashboard summary page
Create `frontend/src/app/dashboard/page.tsx`:
- Watchlist summary cards: each watched ticker with signal, score, brief reasoning
- Market stats: total gainers vs losers, sector distribution
- Recent AI recommendations: latest combined analyses sorted by confidence
- Uses Recharts for:
  - Pie chart: signal distribution (buy/sell/hold)
  - Bar chart: sector performance

### 2.2: Update layout with navbar
Edit `frontend/src/app/layout.tsx`:
- Add Navbar component above {children}
- Ensure proper spacing

### 2.3: Responsive polish
- All pages: test with mobile viewport (375px, 768px)
- Heatmap: becomes scrollable list on mobile
- Charts: full width, reduced height on mobile
- Tables: horizontal scroll on mobile
- Cards: stack vertically on mobile

### 2.4: Build verification
```bash
cd frontend && npm run build
```
Ensure zero TypeScript errors, zero build errors.

### 2.5: Run backend tests
```bash
cd backend && python -m pytest tests/ -v --tb=short
```
Ensure no regressions from CORS or tickers endpoint additions.
</tasks>
