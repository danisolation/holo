---
phase: 27-test-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/app/watchlist/page.tsx
  - frontend/src/app/ticker/[symbol]/page.tsx
  - frontend/src/app/dashboard/paper-trading/page.tsx
  - frontend/src/app/dashboard/portfolio/page.tsx
  - frontend/src/app/dashboard/health/page.tsx
  - frontend/src/app/dashboard/corporate-events/page.tsx
  - frontend/src/components/navbar.tsx
  - frontend/src/components/connection-status.tsx
  - frontend/src/components/candlestick-chart.tsx
  - frontend/src/components/heatmap.tsx
  - frontend/src/components/watchlist-table.tsx
  - frontend/src/components/holdings-table.tsx
  - frontend/src/components/paper-trading/pt-trades-table.tsx
  - frontend/src/components/paper-trading/pt-settings-form.tsx
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "All 8 page routes have page-level data-testid markers for E2E test identification"
    - "Navbar, theme toggle, and connection status have data-testid for navigation testing"
    - "Interactive components (tabs, forms, tables, chart containers) have stable data-testid selectors"
  artifacts:
    - path: "frontend/src/components/navbar.tsx"
      provides: "Navbar with data-testid='navbar' and data-testid='nav-desktop' and data-testid='theme-toggle'"
      contains: "data-testid"
    - path: "frontend/src/components/candlestick-chart.tsx"
      provides: "Chart container with data-testid='candlestick-chart'"
      contains: "data-testid"
    - path: "frontend/src/components/heatmap.tsx"
      provides: "Heatmap container with data-testid='heatmap'"
      contains: "data-testid"
    - path: "frontend/src/app/dashboard/paper-trading/page.tsx"
      provides: "Paper trading tabs with data-testid='pt-tabs' and per-tab testids"
      contains: "data-testid"
  key_links:
    - from: "future E2E tests (Phases 28-31)"
      to: "frontend components"
      via: "page.getByTestId() selectors"
      pattern: "data-testid"
---

<objective>
Add stable `data-testid` attributes to all key UI components for reliable Playwright E2E test selection.

Purpose: Without data-testid, tests rely on brittle CSS selectors or text content that breaks on any UI change. This makes the entire test suite from Phases 28-31 robust and maintainable.
Output: 16 frontend files updated with data-testid attributes on page shells, navigation, tabs, forms, tables, and chart containers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/navbar.tsx
@frontend/src/components/connection-status.tsx
@frontend/src/app/page.tsx
@frontend/src/app/dashboard/page.tsx
@frontend/src/app/watchlist/page.tsx
@frontend/src/app/ticker/[symbol]/page.tsx
@frontend/src/app/dashboard/paper-trading/page.tsx
@frontend/src/app/dashboard/portfolio/page.tsx
@frontend/src/app/dashboard/health/page.tsx
@frontend/src/app/dashboard/corporate-events/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid to page shells and navigation</name>
  <files>
    frontend/src/app/page.tsx,
    frontend/src/app/dashboard/page.tsx,
    frontend/src/app/watchlist/page.tsx,
    frontend/src/app/ticker/[symbol]/page.tsx,
    frontend/src/app/dashboard/paper-trading/page.tsx,
    frontend/src/app/dashboard/portfolio/page.tsx,
    frontend/src/app/dashboard/health/page.tsx,
    frontend/src/app/dashboard/corporate-events/page.tsx,
    frontend/src/components/navbar.tsx,
    frontend/src/components/connection-status.tsx
  </files>
  <read_first>
    - frontend/src/app/page.tsx (returns fragment `<>...</>` — needs wrapper div)
    - frontend/src/app/dashboard/page.tsx (returns `<div className="space-y-8">`)
    - frontend/src/app/watchlist/page.tsx (returns `<div>`)
    - frontend/src/app/ticker/[symbol]/page.tsx (returns `<div className="space-y-6">`)
    - frontend/src/app/dashboard/paper-trading/page.tsx (returns `<div className="space-y-6">`)
    - frontend/src/app/dashboard/portfolio/page.tsx (returns `<div className="space-y-8">`)
    - frontend/src/app/dashboard/health/page.tsx (returns `<div className="space-y-8">`)
    - frontend/src/app/dashboard/corporate-events/page.tsx (returns `<div className="space-y-8">`)
    - frontend/src/components/navbar.tsx (header element at line 39)
    - frontend/src/components/connection-status.tsx (wrapper div at line 48)
  </read_first>
  <action>
**Naming convention:** `data-testid="page-{name}"` for pages, `data-testid="{component}"` for components.

**Page shells — add `data-testid` to the root element of each page:**

1. **`frontend/src/app/page.tsx`** — Currently returns `<>...</>` (React fragment). Replace the opening `<>` with `<div data-testid="page-home">` and the closing `</>` with `</div>`. This wraps all content in a div with the testid.

2. **`frontend/src/app/dashboard/page.tsx`** — The root `<div className="space-y-8">` at the start of the return → add `data-testid="page-dashboard"`:
   ```tsx
   <div data-testid="page-dashboard" className="space-y-8">
   ```

3. **`frontend/src/app/watchlist/page.tsx`** — The root `<div>` → add `data-testid="page-watchlist"`:
   ```tsx
   <div data-testid="page-watchlist">
   ```

4. **`frontend/src/app/ticker/[symbol]/page.tsx`** — The root `<div className="space-y-6">` → add `data-testid="page-ticker-detail"`:
   ```tsx
   <div data-testid="page-ticker-detail" className="space-y-6">
   ```

5. **`frontend/src/app/dashboard/paper-trading/page.tsx`** — The root `<div className="space-y-6">` → add `data-testid="page-paper-trading"`:
   ```tsx
   <div data-testid="page-paper-trading" className="space-y-6">
   ```

6. **`frontend/src/app/dashboard/portfolio/page.tsx`** — The root `<div className="space-y-8">` → add `data-testid="page-portfolio"`:
   ```tsx
   <div data-testid="page-portfolio" className="space-y-8">
   ```

7. **`frontend/src/app/dashboard/health/page.tsx`** — The root `<div className="space-y-8">` → add `data-testid="page-health"`:
   ```tsx
   <div data-testid="page-health" className="space-y-8">
   ```

8. **`frontend/src/app/dashboard/corporate-events/page.tsx`** — The root `<div className="space-y-8">` → add `data-testid="page-corporate-events"`:
   ```tsx
   <div data-testid="page-corporate-events" className="space-y-8">
   ```

**Navigation components:**

9. **`frontend/src/components/navbar.tsx`** — Three additions:
   - The `<header>` element (line 39): add `data-testid="navbar"`:
     ```tsx
     <header data-testid="navbar" className="sticky top-0 z-50 ...">
     ```
   - The desktop `<nav>` element (line 48): add `data-testid="nav-desktop"`:
     ```tsx
     <nav data-testid="nav-desktop" className="hidden md:flex items-center gap-1 ml-4">
     ```
   - The theme toggle `<Button>` (line 78): add `data-testid="theme-toggle"`:
     ```tsx
     <Button
       data-testid="theme-toggle"
       variant="ghost"
       size="icon-sm"
       ...
     ```

10. **`frontend/src/components/connection-status.tsx`** — The wrapper `<div>` (line 48): add `data-testid="connection-status"` and `data-status={status}`:
    ```tsx
    <div
      data-testid="connection-status"
      data-status={status}
      className="flex items-center gap-1.5"
      ...
    ```
  </action>
  <verify>
    <automated>cd frontend && Select-String -Path "src/app/page.tsx" -Pattern 'data-testid="page-home"' && Select-String -Path "src/app/dashboard/page.tsx" -Pattern 'data-testid="page-dashboard"' && Select-String -Path "src/components/navbar.tsx" -Pattern 'data-testid="navbar"' && Select-String -Path "src/components/connection-status.tsx" -Pattern 'data-testid="connection-status"'</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/page.tsx` contains `data-testid="page-home"`
    - `frontend/src/app/dashboard/page.tsx` contains `data-testid="page-dashboard"`
    - `frontend/src/app/watchlist/page.tsx` contains `data-testid="page-watchlist"`
    - `frontend/src/app/ticker/[symbol]/page.tsx` contains `data-testid="page-ticker-detail"`
    - `frontend/src/app/dashboard/paper-trading/page.tsx` contains `data-testid="page-paper-trading"`
    - `frontend/src/app/dashboard/portfolio/page.tsx` contains `data-testid="page-portfolio"`
    - `frontend/src/app/dashboard/health/page.tsx` contains `data-testid="page-health"`
    - `frontend/src/app/dashboard/corporate-events/page.tsx` contains `data-testid="page-corporate-events"`
    - `frontend/src/components/navbar.tsx` contains `data-testid="navbar"` AND `data-testid="nav-desktop"` AND `data-testid="theme-toggle"`
    - `frontend/src/components/connection-status.tsx` contains `data-testid="connection-status"` AND `data-status={status}`
  </acceptance_criteria>
  <done>All 8 pages have page-level data-testid markers, navbar has 3 data-testid attributes, connection-status has data-testid with status attribute</done>
</task>

<task type="auto">
  <name>Task 2: Add data-testid to interactive components</name>
  <files>
    frontend/src/components/candlestick-chart.tsx,
    frontend/src/components/heatmap.tsx,
    frontend/src/components/watchlist-table.tsx,
    frontend/src/components/holdings-table.tsx,
    frontend/src/components/paper-trading/pt-trades-table.tsx,
    frontend/src/components/paper-trading/pt-settings-form.tsx,
    frontend/src/app/dashboard/paper-trading/page.tsx
  </files>
  <read_first>
    - frontend/src/components/candlestick-chart.tsx (outer return div at line 291: `<div className="space-y-2">`, chart ref div at line 367)
    - frontend/src/components/heatmap.tsx (outer return div at line 74: `<div className="space-y-4">`)
    - frontend/src/components/watchlist-table.tsx (main return Table at line 283)
    - frontend/src/components/holdings-table.tsx (main return Card at line 215)
    - frontend/src/components/paper-trading/pt-trades-table.tsx (main return Card at line 230)
    - frontend/src/components/paper-trading/pt-settings-form.tsx (main return Card at line 40)
    - frontend/src/app/dashboard/paper-trading/page.tsx (Tabs at line 26, TabsTrigger elements at lines 28-32)
  </read_first>
  <action>
**Chart containers:**

1. **`frontend/src/components/candlestick-chart.tsx`** — Two additions:
   - The outer wrapper `<div className="space-y-2">` (line 291) → add `data-testid="candlestick-chart"`:
     ```tsx
     <div data-testid="candlestick-chart" className="space-y-2">
     ```
   - The chart canvas container `<div ref={containerRef} className="rounded-xl overflow-hidden" />` (line 367) → add `data-testid="chart-canvas"`:
     ```tsx
     <div data-testid="chart-canvas" ref={containerRef} className="rounded-xl overflow-hidden" />
     ```

2. **`frontend/src/components/heatmap.tsx`** — The main return `<div className="space-y-4">` (line 74) → add `data-testid="heatmap"`:
   ```tsx
   <div data-testid="heatmap" className="space-y-4">
   ```

**Tables:**

3. **`frontend/src/components/watchlist-table.tsx`** — The `<Table>` element in the main return (line 283) → add `data-testid="watchlist-table"`:
   ```tsx
   <Table data-testid="watchlist-table">
   ```

4. **`frontend/src/components/holdings-table.tsx`** — The `<Card>` wrapper in the main return (line 215) → add `data-testid="holdings-table"`:
   ```tsx
   <Card data-testid="holdings-table">
   ```

5. **`frontend/src/components/paper-trading/pt-trades-table.tsx`** — The `<Card>` wrapper in the main return (line 230) → add `data-testid="trades-table"`:
   ```tsx
   <Card data-testid="trades-table">
   ```

**Forms:**

6. **`frontend/src/components/paper-trading/pt-settings-form.tsx`** — The `<Card>` wrapper in the main return (line 40) → add `data-testid="settings-form"`:
   ```tsx
   <Card data-testid="settings-form">
   ```

**Tabs (paper trading page):**

7. **`frontend/src/app/dashboard/paper-trading/page.tsx`** — This file was already modified in Task 1 (page-level testid). Now add tab-specific testids:
   - The `<Tabs>` element (line 26) → add `data-testid="pt-tabs"`:
     ```tsx
     <Tabs data-testid="pt-tabs" defaultValue="overview">
     ```
   - Each `<TabsTrigger>` → add `data-testid="pt-tab-{value}"`:
     ```tsx
     <TabsTrigger data-testid="pt-tab-overview" value="overview">Tổng quan</TabsTrigger>
     <TabsTrigger data-testid="pt-tab-trades" value="trades">Lệnh</TabsTrigger>
     <TabsTrigger data-testid="pt-tab-analytics" value="analytics">Phân tích</TabsTrigger>
     <TabsTrigger data-testid="pt-tab-calendar" value="calendar">Lịch</TabsTrigger>
     <TabsTrigger data-testid="pt-tab-settings" value="settings">Cài đặt</TabsTrigger>
     ```
   - Each `<TabsContent>` → add `data-testid="pt-content-{value}"`:
     ```tsx
     <TabsContent data-testid="pt-content-overview" value="overview">
     <TabsContent data-testid="pt-content-trades" value="trades">
     <TabsContent data-testid="pt-content-analytics" value="analytics">
     <TabsContent data-testid="pt-content-calendar" value="calendar">
     <TabsContent data-testid="pt-content-settings" value="settings">
     ```
  </action>
  <verify>
    <automated>cd frontend && Select-String -Path "src/components/candlestick-chart.tsx" -Pattern 'data-testid="candlestick-chart"' && Select-String -Path "src/components/heatmap.tsx" -Pattern 'data-testid="heatmap"' && Select-String -Path "src/components/watchlist-table.tsx" -Pattern 'data-testid="watchlist-table"' && Select-String -Path "src/app/dashboard/paper-trading/page.tsx" -Pattern 'data-testid="pt-tabs"' && Select-String -Path "src/components/paper-trading/pt-settings-form.tsx" -Pattern 'data-testid="settings-form"'</automated>
  </verify>
  <acceptance_criteria>
    - `candlestick-chart.tsx` contains `data-testid="candlestick-chart"` AND `data-testid="chart-canvas"`
    - `heatmap.tsx` contains `data-testid="heatmap"`
    - `watchlist-table.tsx` contains `data-testid="watchlist-table"`
    - `holdings-table.tsx` contains `data-testid="holdings-table"`
    - `pt-trades-table.tsx` contains `data-testid="trades-table"`
    - `pt-settings-form.tsx` contains `data-testid="settings-form"`
    - `paper-trading/page.tsx` contains `data-testid="pt-tabs"` AND all 5 `data-testid="pt-tab-*"` AND all 5 `data-testid="pt-content-*"`
    - Total `data-testid` count across all modified files: `Select-String -Path "src" -Pattern "data-testid" -Recurse` returns 30+ matches
  </acceptance_criteria>
  <done>All chart containers, tables, forms, and tab elements have stable data-testid attributes ready for Playwright getByTestId() selectors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan only adds HTML attributes — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Information Disclosure | data-testid attributes | accept | `data-testid` attributes are visible in production HTML. They contain no sensitive info (just component names). Standard practice for testable apps. Could be stripped in production build if desired (future scope). |
</threat_model>

<verification>
1. `Select-String -Path frontend/src -Pattern "data-testid" -Recurse | Measure-Object` returns 30+ matches
2. Every page has exactly one `data-testid="page-*"` attribute
3. Navbar has `data-testid="navbar"`, `data-testid="nav-desktop"`, `data-testid="theme-toggle"`
4. Paper trading page has `data-testid="pt-tabs"` with 5 tab triggers and 5 tab contents
5. `cd frontend && npx next build` still succeeds (no JSX errors introduced)
</verification>

<success_criteria>
- 30+ data-testid attributes added across 16 files
- All 8 pages identifiable via `page.getByTestId('page-*')`
- Navigation, tabs, forms, tables, and charts identifiable via getByTestId
- No functional or layout regressions (build still passes)
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-infrastructure-foundation/27-02-SUMMARY.md`
</output>
